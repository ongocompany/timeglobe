# 개발 기본 지침서: TimeGlobe 기획 및 개발 컨텍스트

## 1. 프로젝트 개요
* **프로젝트명:** TimeGlobe (가칭)
* **목적:** 지질학적 시대부터 인류 역사까지 지구의 공간적·시간적 변화를 3D 지구본과 타임라인으로 탐험하는 인터랙티브 에듀테크 서비스.
* **타겟 사용자:** 역사/과학 관심층, 학생, 교육 종사자 및 자녀를 둔 학부모.
* **개발 방식:** 1인 개발, AI 코딩 도구(바이브 코딩) 적극 활용.
* **플랫폼:** 반응형 웹(Web) 우선 개발 후, 향후 모바일 앱(App)으로 패키징 확장 고려.

## 2. 주요 기술 스택
* **프론트엔드:** Next.js (확정)
* **3D 렌더링:** `Globe.gl` (우주 배경, 3D 지구본, 마커, 텍스처 맵 및 GeoJSON 국경선 표현)
* **백엔드/데이터베이스:** Supabase (동적 데이터 관리, 사용자 인증 및 결제 정보 처리)
* **배포:** Vercel (웹) / Capacitor 또는 PWA (모바일 앱)

## 3. 핵심 차별점 및 비즈니스 모델
* **통합된 경험:** 기존 서비스들이 분리해 놓은 '지질 시대(대륙 이동)'와 '인류 역사(문명/국경)'를 3D 지구본 하나로 매끄럽게 연결.
* **선택과 집중 (큐레이션):** 방대한 데이터를 단순 나열하지 않고, 핵심 사건 위주의 직관적인 마커 제공. 서구권 중심에서 벗어나 삼국시대, 當時(당시)의 이백/두보 등 동양사 데이터의 디테일 강화.
* **수익 모델 (Freemium):** MVP 제한 노출(예: 서기 0~1500년 구간 무료) 후, 지질 시대 및 근현대사 풀 콘텐츠는 '일회성 결제(Time-Travel Pass)'로 잠금 해제.
* **게이미피케이션 (학부모 타겟 결제 유도):** 
    * 타임 트래블러 패스포트 (도감 스탬프 수집).
    * 인터랙티브 역사 퀴즈 (위치 찾기 등) 및 부모님용 학습 진척도 확인 기능.
    * 유저 레벨링 시스템 (`travel_tokens` 기반 '초보 타임트래블러' -> '타임로드' 등급 부여)

## 4. UI 및 데이터 시각화 전략
* **마커 카테고리:** 정치/전쟁(빨강), 인물/문화(노랑), 과학/발명(파랑), 건축/유물(초록), 자연재해/지질(회색) 등으로 시각적 구분.
* **불명확한 지역:** 중남미 문명 등 국경이 모호한 곳은 Heatmap 스타일의 원형 마커나 안개(Fog) 효과로 뉘앙스만 표현.
* **몰입감 강화:** 시대/지역별 동적 배경음악 적용 및 화산 폭발 등 주요 이벤트 파티클 이펙트.
* **다국어 지원:** JSONB를 활용한 핵심 텍스트 데이터 다국어 확장성 확보.

## 5. 팝업 모달(Modal) 필수 구성 요소 (데이터베이스 설계 기준)
해당 마커를 클릭했을 때 나타나는 상세 정보 창.
* **헤더:** 이벤트 제목, 발생 연도/시대, 카테고리 태그.
* **본문:** 시각적 몰입을 위한 대표 이미지 1장, 핵심 요약 설명(3~4줄), 현재 국가/지역명 텍스트.
* **액션(하단):** 나무위키 등 외부 링크 버튼, 도감(패스포트) 스탬프 획득 알림, 미니 퀴즈 시작 버튼, 관련 시대 이벤트 추천.

---

## 6. 기술 검토 및 추가 제안 (2026-02-24)

### 6-1. [cl] 프론트엔드 프레임워크 결정 (Next.js 채택)
현재 "React (또는 Next.js)"로 미정 상태. **Next.js 확정을 권장**하는 이유:
* **SEO**: 에듀테크 서비스 특성상 "삼국시대 역사", "로마제국 지도" 등 검색 유입이 중요. SPA만으로는 SEO가 약함.
* **Vercel 배포 궁합**: Next.js + Vercel은 같은 회사 제품으로 배포가 압도적으로 편리.
* **SSG 활용**: Events, Eras 등 정적 데이터는 빌드 시 미리 생성(Static Site Generation)해두면 초기 로딩 속도 향상.
**-> [jn/gm/cl 합의 완료] Next.js 도입 확정.**

### 6-2. [gm] Globe.gl 성능 리스크 및 오픈소스 대안 분석
Globe.gl은 WebGL(Three.js) 기반이라 수백 개의 마커와 무거운 GeoJSON 폴리곤(국경선) 동시 렌더링 시 모바일 버벅임이나 메모리 이슈가 생길 위험이 높습니다. 이를 대체할 만한 고성능 지도 렌더링 오픈소스 라이브러리는 다음과 같습니다.

1. **CesiumJS (새로운 대안 1순위)**
   * **특징**: 고정밀 3D 지구본/지도 렌더링의 끝판왕 격인 오픈소스.
   * **장점**: 막강한 리얼리즘(기획에 맞는 실제 지구/지형 느낌), 정교한 3D 데이터 스트리밍 특화.
   * **단점**: 라이브러리가 무겁고, React 연동(`Resium`)이 다소 까다로우며, 거대한 GeoJSON은 3D Tiles 포맷 변환이 권장됨.

2. **Globe.gl 유지 + 극한의 최적화 (대안 2순위)**
   * **특징**: 기존 기획안의 라이브러리. 우주 배경 등 미적 디자인은 뛰어나오나 성능 리스크 잔존.
   * **장점**: 초기 세팅과 React 연동이 매우 쉬움.
   * **단점**: 성능 한계를 프론트엔드 최적화 기법(Lazy Loading, GeoJSON 단순화, Web Worker 연산 등)으로 직접 극복해야 함.

~~3. deck.gl~~ -> **[jn 검토 결과: 기각]** 데모 확인 결과 '카드 그래픽' 같은 딱딱한 시각성이 프로젝트의 몰입감 있는 에듀테크 방향성과 맞지 않아 도입 전면 취소.

**-> [gm의 제안 및 DB 스키마 수정안]**
* 시각적 몰입감이 매우 중요한 프로젝트이므로, 일단 리얼리즘 최상급인 **CesiumJS**로 렌더링 PoC(사전 검증)를 진행하거나, 디자인이 예쁜 기존 **Globe.gl을 살리되 최적화(CDN 등)를 강하게 적용**하는 두 가지 방향으로 좁히는 것이 맞습니다.
* **DB 스키마 구조 (CDN 호스팅 유지)**: 어떤 라이브러리를 쓰든 대규모 GeoJSON 텍스트를 Supabase DB(`jsonb`)에서 매번 꺼내오는 건 위험합니다. 따라서 `Borders` 테이블의 `geojson_data(jsonb)` 필드를 삭제하고, **외부 CDN에 정적 파일(`.geojson`)을 올려둔 뒤 그 주소를 가리키는 `geojson_url(text)` 컬럼으로 변경한 것은 그대로 유지합니다.**

### 6-3. [cl] 결제 시스템 및 기타 사항 (보류)
결제 시스템(Stripe, 토스페이먼츠 등), PWA 도입 여부, 다국어 i18n 라이브러리 선정 등은 MVP 핵심 기능 구현 후 결정하기로 보류.

### 6-5. 추가 고려 사항
* **다국어 i18n 라이브러리**: DB의 JSONB 다국어 데이터와 별도로, 프론트엔드 UI 텍스트(버튼, 메뉴 등) 번역을 위해 `next-intl` 등 라이브러리 사전 선정 필요.
* **이미지 호스팅 전략**: Events의 `image_url` 저장소 미정.
    * Supabase Storage (간편, 무료 1GB 제한) / Cloudflare R2 (비용 효율적) / 위키미디어 커먼즈 외부 URL 활용 (MVP 단계, 저작권 확인 필요).

### 6-6. MVP 개발 우선순위 제안

| 순서 | 기능 | 이유 |
|:---:|------|------|
| 1 | Next.js + Globe.gl 기본 세팅 | 3D 지구본 렌더링 확인 (동기부여) |
| 2 | 타임라인 슬라이더 + 마커 표시 | 핵심 인터랙션 구현 |
| 3 | 마커 클릭 → 모달 팝업 | 콘텐츠 확인 가능 |
| 4 | Supabase 연동 (Events 데이터) | 정적 → 동적 데이터 전환 |
| 5 | 인증 + 도감(패스포트) | 사용자 기능 시작 |
| 6 | 퀴즈 / 결제 / BGM 등 | 부가 기능 (후순위) |

### 6-7. [cl] CesiumJS 커스터마이징 가능 범위 조사 (2026-02-24)

진형(jn)의 요청으로 CesiumJS에서 TimeGlobe가 요구하는 시각적 커스터마이징이 어디까지 가능한지 조사한 결과입니다.

#### 3D 오브젝트 마커 (glTF 모델) — 완벽 지원
CesiumJS는 마커 위치에 **glTF/GLB 포맷의 3D 모델**을 직접 배치할 수 있습니다.
* 카테고리별 3D 마커 구현 가능 (예: 전쟁 = 대포, 발명 = 전구/느낌표 등)
* glTF에 내장된 **애니메이션 재생** 지원 (등장 시 뿅 하고 튀어나오는 연출 가능)
* 애니메이션 속도 조절, 반복, 역재생 모두 가능
* Entity 클릭 이벤트 지원 → 마커 클릭 시 모달 팝업 연동 가능
* 일반 2D 아이콘(Billboard) 마커도 동시 지원
* 무료 glTF 모델 소스: [Sketchfab](https://sketchfab.com), [Poly Pizza](https://poly.pizza) 등
* 참고: [CesiumJS ModelGraphics 공식 문서](https://cesium.com/learn/cesiumjs/ref-doc/ModelGraphics.html)

#### 우주 배경 (SkyBox) 커스텀 — 완벽 지원
기본 배경이 밋밋하더라도 **SkyBox 큐브맵 6면 이미지**를 교체하여 완전히 커스텀 가능합니다.
* 성운, 은하수, 별이 가득한 우주 배경으로 자유롭게 교체 가능
* 원리: 지구본을 감싸는 거대한 정육면체 6면에 이미지를 입히는 방식
* NASA 등에서 무료 우주 이미지 배포 중, 직접 제작한 이미지도 사용 가능
* 참고: [CesiumJS SkyBox 공식 문서](https://cesium.com/learn/cesiumjs/ref-doc/SkyBox.html)

#### 파티클 이펙트 (화산, 폭발 등) — 내장 기능
CesiumJS에는 **ParticleSystem**이 내장되어 있어 별도 라이브러리 없이 이펙트 구현 가능합니다.
* 기본 제공 이펙트: 불/연기(화산), 폭발(전쟁), 불꽃놀이, 눈/비/먼지
* 색상 그라데이션 (시작색 → 끝색 전환)
* 크기 변화 (작게 시작 → 크게 퍼짐)
* **버스트(폭발) 타이밍 지정** 가능
* 파티클 텍스처 이미지 자유 교체
* 참고: [CesiumJS Particle Systems 튜토리얼](https://cesium.com/learn/cesiumjs-learn/cesiumjs-particle-systems/)

#### 기타 내장 기능
* **Timeline 위젯**: 시간축 슬라이더 내장 (TimeGlobe의 타임라인 기능에 활용)
* **GeoJsonDataSource**: GeoJSON 국경선 데이터 렌더링 지원
* **시간 기반 애니메이션(CZML)**: 시간에 따라 데이터가 변하는 시각화에 최적화

#### 커스터마이징 가능 범위 요약

| TimeGlobe 요구사항 | CesiumJS 지원 여부 | 비고 |
|---|---|---|
| 카테고리별 3D 마커 (대포, 전구 등) | **glTF 모델 로딩으로 완벽 지원** | 무료 모델 소스 풍부 |
| 마커 등장 애니메이션 (뿅!) | **glTF 내장 애니메이션 재생** | 속도/반복/역재생 제어 가능 |
| 마커 클릭 → 모달 | **Entity 클릭 이벤트** | 기본 API 지원 |
| 우주 느낌 배경 | **SkyBox 큐브맵 커스텀** | 6면 이미지 자유 교체 |
| 화산 폭발/전쟁 이펙트 | **ParticleSystem 내장** | 불/연기/폭발/눈/비 등 |
| 타임라인 슬라이더 | **Timeline 위젯 내장** | 시간 기반 데이터 전환 최적화 |
| GeoJSON 국경선 | **GeoJsonDataSource** | CDN에서 fetch 후 렌더링 |

**-> [jn 검토 대기] CesiumJS는 TimeGlobe가 요구하는 모든 시각적 커스터마이징을 기본 기능으로 지원함. 3D 렌더링 라이브러리 최종 확정 논의 필요.**
