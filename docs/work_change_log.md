# TimeGlobe Work Change Log

*이 문서는 프로젝트의 주요 변경 사항과 AI 어시스턴트(Claude, Gemini 등)의 작업 내역을 추적하기 위해 사용됩니다.*
*작업자는 대량 데이터 수정 시, 진형의 지시 시, 또는 업무 종료 시에 이 문서에 변경 내역을 기록해야 합니다.*

## [2026-03-27] [cl] 세션 7: events+architecture 대규모 품질 검수 및 보강

### 작업 내용

#### 1. events 설명 보강 (sonnet 병렬 + opus 재검증)
- 요약=설명 중복 4,624건 → 14건 (99.7% 해소)
- sonnet 에이전트 24배치 병렬 처리 후 opus 15배치 재검증
- 연도 교정 674건 (start_year/end_year 오류, BCE/CE 부호 혼동 등)
- region.en 한글→영어 변환 1,660건
- start_year null 9건 전부 해소

#### 2. architecture 설명 보강 (sonnet 병렬 + opus 재검증)
- 요약=설명 중복 1,217건 → 1건 (99.9% 해소)
- sonnet 에이전트 7배치 처리 후 opus 3배치 재검증
- 연도 교정 213건 + 별도 검증 169건
- start_year null 2건 해소 (부다성 1247, 난징 총통부 1368)

#### 3. opus 재검증 (전체 8,625건)
- sonnet 작성물 전수 검증 → 1,287건 플래그 (14.9%)
- 사실 오류 214건 발견 → 112건 수정 (할루시네이션, 건물/사건 혼동)
  - 주요: 바축 학살(다른 사건), 고자 폭동(이스라엘→오키나와), 2002 월드컵(독일 격파→패배)
  - 건물 혼동: 토 궁전(통가→랭스), 섬 위의 궁전(완쿠트→와지엔키)
- 날짜 오류 339건 발견 → 169건 수정
- 경어체 282건 스크립트 일괄 제거
- 잔여 중복 67건 opus로 재작성

#### 4. persons 연도 검증
- 사망연도 미상(1920년 이전) 144건 → 108건 확정, 36건 리뷰 필요
- start_year 교정 4건 (Tirso de Molina 1837→1579 등)

### 최종 결과
- **events**: 7,238개 (요약=설명 중복 0.2%, 경어체 0건, start_year null 0건)
- **architecture**: 1,387개 (요약=설명 중복 0.1%, 경어체 0건, start_year null 0건)
- **잔여 중복 15건**: archive 분리 (public/data/archive/)

### 잔여 과제
- persons 요약=설명 중복 15,540건 (94%) 설명 보강 ← 다음 작업
- persons 국적 미매칭 1,905건
- persons 유럽 편중 56.7% → 비서양권 추가 보강

#### 7. persons 설명 보강 (진행중, batch 1~31 완료)
- 요약=설명 중복 15,540건 중 5,800건 처리 → 잔여 11,920건 (72%)
- 연도 교정 241건, 지역 교정 60건
- 동명이인 혼동 다수 발견 (솔로몬/헝가리왕, 가이우스/카이사르 등)
- 좌표 오류 다수 (베를린→보스턴, 카이로→프랑스, 카라카스→산티아고 등)
- 미완료: batch 25, 28, 32 + batch 33~78

### 최종 결과 (세션 7 시점)
- **events**: 7,238개 (요약=설명 중복 0.2%, 경어체 0건)
- **architecture**: 1,387개 (요약=설명 중복 0.1%, 경어체 0건)
- **persons**: 16,411개 (요약=설명 중복 72% → 37.3% 처리 진행중)

### 주요 교훈
- sonnet으로 빠르게 1차 작성 → opus로 재검증 2단계 파이프라인이 효과적
- sonnet 할루시네이션: 잘 모르는 사건을 자신있게 다른 사건으로 기술하는 패턴 확인
- 대량 배치는 jinserver tmux에서 실행 권장 (맥북 슬립 방지)
- 기존 데이터 자체의 오염이 심각 (동명이인, 좌표 오류, 지역 태깅 등)

---

## [2026-03-24] [cl] 세션 6: 전 카테고리 대규모 보강 + 구조 개편 + 품질 정리

### 작업 내용

#### 1. events 대규모 보강: +2,795건 (4,443 → 7,238)
- 한국 중복 48건 제거
- 미태깅 1,121건 + 도시명 664건 좌표 기반 지역 자동 분류
- 12개 지역 전부 보강 (동아시아 +1,471 / 북미 +281 / 남아시아 +368 / 일본 +157 등)
- 병렬 에이전트 6~8개 동시 활용

#### 2. artworks 보강 + 품질 정리: 2,392 → 2,592건
- 비서양권 12개 지역 +971건 추가
- 비artworks 제거 (게임 ~200건, 성경/신화/개념/도시 등 총 ~470건 → archive)
- 할루시네이션 설명 ~400건 탐지 및 초기화
- 경어체→평서문 2,910건 변환
- 밴드/인물/장르/악기 155건 제거 (원칙: 눈에 보이는 실체만)

#### 3. places/inventions/nations 보강: +347건
- places 1,217→1,387 (+170), inventions 199→298 (+99), nations 174→252 (+78)
- 전 항목 좌표 기반 지역 태깅 완료

#### 4. 카테고리 구조 대폭 개편
- **활성 4개**: persons / events / architecture / nations
- **archive 6개**: artworks / films / inventions / items / places(원본)
- places → architecture로 리브랜딩 (1,387개)
- artworks 기준 확립: "눈에 보이는 실체" (앨범/악보/책/그림/건물/필름)

#### 5. 전 카테고리 품질 정리
- **persons**: wikidata dump 기반 국적→지역 교정 684건 (1,300만명 dump 활용)
- **persons**: 지역 태깅 12,456건 + 표준화 3,921건 + 할루시네이션 43건
- **events**: 경어체 1,094건 변환
- **architecture**: 경어체 1,199건 변환
- **전 카테고리 경어체 0건 달성**

#### 6. 데이터 리뷰 UI 개발 (/data-review)
- 4개 카테고리 JSON 기반 카드 뷰어
- 대륙/시대/카테고리/신규·기존 필터 + 텍스트 검색 + 6가지 정렬
- 상세 패널 sticky 고정 + 제목·ID 복사 버튼
- **신고 기능**: 분류오류/설명부실/삭제필요/제목수정/기타 (localStorage 저장)

### 최종 결과
- **persons**: 16,411개 (지역 100% 태깅, 국적 기반 교정 완료)
- **events**: 7,238개 (지역 100% 태깅, 경어체 0건)
- **architecture**: 1,387개 (지역 100% 태깅, 경어체 0건)
- **nations**: 252개
- **활성 총**: 25,288개 (4개 카테고리)

### 잔여 과제
- events 요약=설명 3,420건 (47%) 설명 보강
- architecture 요약=설명 1,217건 (88%) 설명 보강
- persons 국적 미매칭 1,905건 (QID 없는 건)
- persons 유럽 편중 56.7% → 비서양권 추가 보강
- artworks 재활용 검토 (archive에서 양질 데이터 선별)

### 주요 피드백 반영
- AI 생성 설명은 사실 기반만 허용 (수식어 나열 금지)
- 경어체(습니다/합니다) 금지, 평서문(이다/했다) 통일
- artworks 기준: 시간·공간 확정 가능한 실체만 (무형문화/신화/개념 제외)
- 국적/출생지 기준으로 지역 분류 (활동지 아님)

---

## [2026-03-22~23] [cl] 세션 5: 전 세계 인물 데이터 편중 분석 + 대규모 보강

### 작업 내용

#### 1. 데이터 편중 분석 (data_status.md v0.5.0 → v0.6.0)
- 8개 카테고리 전체 대륙별/시대별/카테고리별 분석 수행
- 유럽+북미 70% 편중, 남아시아 0.5%, 아프리카 2.0% 심각 부족 확인
- 근대(1800~1945) 50% 과다, 고대/선사 2.5% 부족 확인

#### 2. 중복/이벤트 제거: -209건
- 전체 persons 중복 144건 제거 (제목 정규화 기반)
- 한국 이벤트 혼입 22건 분리 (3·1운동, 5·18, IMF 등)
- 한국 추가 중복 43건 제거 (왕건 4중복, 이방원 3중복 등)
- 중복제거 스크립트: `scripts/dedup_persons.js`

#### 3. 지역별 인물 보강: +325명 (병렬 에이전트 활용)

| 지역 | 추가 | 시대 커버리지 |
|------|------|-------------|
| 중국 | +68 | 삼황오제(선사) → 당나라시인 → 송원 → 삼전과학자(현대) |
| 한국 | +66 | 해모수(부여) → 김유신 → 안향/만적(고려) → 장기려/김학순(현대) |
| 일본 | +24 | 야마토타케루 → 운케이 → 스기타겐파쿠 → 오타니쇼헤이 |
| 동남아(6개국) | +63 | 쯩자매(베트남고대) → 앙코르왕 → 쩐년톤 → 당타이선(현대) |
| 인도 | +49 | 라마/크리슈나(신화) → 수슈루타 → 바바르 → 순다르피차이 |
| 중앙아시아 | +17 | 다리우스 → 알콰리즈미 → 하피즈/사디 → 키아로스타미 |
| 중동 | +15 | 길가메시 → 함무라비 → 이븐알하이삼 → 오르한파묵 |
| 아프리카 | +17 | 임호텝 → 에자나왕 → 만사무사 → 움쿨숨/아베베비킬라 |

#### 4. 기타 작업
- jinserver_sync 미병합 데이터 통합 (+65 persons)
- 병합 스크립트: `scripts/merge_sync_data.js`
- data_status.md v0.6.0 업데이트

### 최종 결과
- **persons**: 16,295 → 16,411개 (+116 순증, 품질 대폭 향상)
- **전체**: 25,912 → 25,976개

### 잔여 과제
- 유럽 비중 55.9% → 비서양권 추가 보강 필요
- events/places/inventions/nations 카테고리 보강 미착수
- 카테고리 세분화 (인물/문화 87% → 정치/학자/예술 분류)
- modern_country/historical_region 미분류 정리

---

## [2026-03-21] [cl] 세션 4: 한국 인물 카드 800개 완성 — persons_korea_new.json

### 작업 내용

#### 1. 한국 역사 인물 카드 800개 생성 완료
- **파일**: `public/data/jinserver_sync/persons_korea_new.json`
- **총 항목**: 800개 (KR_001 ~ KR_800)
- **대상**: 고조선부터 현대까지 한국 역사 인물 전 시대 커버
- **2개 세션에 걸쳐 64개 임시 파일 (/tmp/persons_korea_part*.json) 후 최종 병합**

#### 2. 카테고리 분포 (800개)
| 카테고리 | 건수 |
|---------|------|
| 인물/문화 | 299 |
| 인물/정치 | 245 |
| 인물/군사 | 86 |
| 인물/스포츠 | 53 |
| 인물/경제 | 40 |
| 인물/과학 | 33 |
| 인물/종교 | 44 |
| **합계** | **800** |

#### 3. 시대 분포
| 시대 | 건수 |
|------|------|
| era-contemporary | 350 |
| era-early-modern | 183 |
| era-modern | 129 |
| era-medieval | 92 |
| era-classical | 38 |
| era-ancient | 8 |

#### 4. 주요 커버 범위
- 고조선: 단군왕검·위만 등 건국 신화 인물
- 삼국시대: 고구려·백제·신라 왕들, 원효·의상 등 고승
- 고려시대: 건국부터 무신정권·몽골 항쟁까지
- 조선시대: 세종·이순신·이황·이이·장영실 등 핵심 인물 전체
- 일제강점기: 안중근·김구·유관순·윤봉길 등 독립운동가 대거 포함
- 현대: 대통령 전원·K팝·영화·스포츠·경제 인물 망라

### 기술 노트
- 세션 컨텍스트 한계 초과로 2개 세션에 걸쳐 작업
- 배치당 10개씩 /tmp/ 임시 파일로 분할 저장 후 Python3로 최종 병합
- 중복 ID 없음, JSON 유효성 검증 완료
- 영문·국문 이중 언어 필드 완비

### 남은 작업
1. persons_korea_new.json을 기존 persons 파이프라인에 통합
2. 한국 인물 데이터 중복 검토 (기존 persons_kr_batch*.json과 비교)
3. 이미지 URL 보충 (현재 모두 빈 값)

---

## [2026-03-17] [cl] 세션 3: 데이터 대폭 확충 — 8개 카테고리 24,666개 카드

### 작업 내용

#### 1. 신규 카테고리 추가
- **films_cards.json**: 470편 신규 생성 (1920~2025, AI 생성 영화 목록 + 좌표 매핑)
- **items_cards.json**: 122건 신규 생성 (역사 유물/랜드마크, 연도+좌표 AI 보충)

#### 2. persons 확충 (15,200 → 15,649)
- 큐레이션 통과 but 카드 미생성 449명 → 소넷 에이전트 5배치 병렬로 카드 생성
- 좌표 매핑: birthplace_coords(29) + country_coords(116) + AI 추정(304)
- **active_start 필드 추가**: 1900+ 출생 인물 4,464명에 주 활동 시작 연도 추가 (박정희 1917→1961 등)
- birth_year 없는 92명 제외 (초대 교황 등)

#### 3. events 대폭 확충 (2,357 → 4,443)
- 기존 문제: 전쟁/전투 편중, 비군사 이벤트 부재 (한국 47건 중 거의 전부 전쟁)
- 소넷 에이전트 11개+ 병렬로 지역별/시대별 이벤트 생성
- 중복 제거 후 2,086건 신규 추가

**이벤트 신규 지역별 분포:**
| 지역 | 신규 건수 |
|------|---------|
| 한국 | 988건 |
| 중국 | 375건 |
| 아프리카 | 131건 |
| 유럽(비군사) | 130건 |
| 중동 | 128건 |
| 중남미 | 121건 |
| 남아시아 | 73건 |
| 일본 | 57건 |
| 북미 | 42건 |
| 동남아 | 28건 |
| 오세아니아 | 13건 |

#### 4. 데이터 편향 분석 및 문제 파악
- 인물 지역 분포: 유럽 58.7%, 한국 1.1% → 심각한 편향 확인
- 한국 인물 178명, 아프리카 325명, 남/동남아 82명 → 대폭 보강 필요
- 나무위키 DB (jinserver `/mnt/data2/namuwiki/namuwiki.db`, 795K 문서) 활용 방안 파악

#### 5. 최종 데이터 현황
| 카테고리 | 건수 | 파일 | 용량 |
|---------|------|------|------|
| persons | 15,649 | persons_cards.json | 32.2MB |
| events | 4,443 | events_cards.json | 7.9MB |
| artworks | 2,392 | artworks_cards.json | 2.9MB |
| places | 1,217 | places_cards.json | 1.8MB |
| films | 470 | films_cards.json | 746KB |
| nations | 174 | nations_cards.json | 267KB |
| inventions | 199 | inventions_cards.json | 281KB |
| items | 122 | items_cards.json | 226KB |
| **합계** | **24,666** | | **~46.1MB** |

### 기술 노트
- Gemini API 대신 **Claude 소넷/하이쿠 에이전트 병렬 실행**으로 대량 데이터 생성 → 비용 절감, 속도 향상
- 좌표 매핑도 AI 에이전트로 처리 (Nominatim 불필요)
- 변환 스크립트: `scripts/wikidata/syncToMockEvent.py`, `scripts/wikidata/mergeNewEvents.py`

### 남은 작업 (다음 세션)
1. **인물 지역 편향 해소** — 한국 1,000명+, 인도/아프리카/중남미 각 수백 명 추가 (최우선)
2. **나무위키 파이프라인** — 한국사 인물/사건 대량 추출
3. **artworks/inventions 확충** — 현재 너무 적음
4. **프론트엔드 연동** — films, items 로딩 + active_start 기반 렌더링
5. **이벤트 교과서 커버리지 검증** — 핵심 사건 누락 체크

---

## [2026-03-13] [cl] 세션 2: 6개 카테고리 21,539개 데이터 카드 완성

### 작업 내용

#### 1. events 확장 (1,237 → 2,357)
- events_raw.jsonl 79,270개에서 키워드(battle, war, revolution 등) + sl≥10 → 4,092 후보
- Gemini 2.5 Flash AI 필터 → **983개** 세계사적 이벤트 채택 (23.6%)
- 추가: no-keyword sl≥15 AI 필터 → **137개** 비전쟁 이벤트 (재해, 탐험, 외교)
- 기존 1,237 + 983 + 137 = **2,357개** events_cards.json (5.1MB)

#### 2. inventions 생성 (199개 신규)
- inventions_raw.jsonl은 정부기관/조직으로 오염 → raw 사용 포기
- Gemini 직접 생성: 10개 시대/분야 카테고리별 발명/발견 요청
- **199개** inventions_cards.json (바퀴, 인쇄술, 증기기관, DNA 등)

#### 3. nations 생성 (174개 신규)
- hist_entities_raw.jsonl 69,498개에서 sl≥20 → 1,955 후보
- Gemini AI 필터 → **174개** 국가/제국/문명 채택 (8.5%)
- nations_cards.json: 우르, 로마제국, 조선, 오스만 등

#### 4. 최종 데이터 현황
| 카테고리 | 건수 | 파일 |
|---------|------|------|
| persons | 15,200 | persons_cards.json (31MB) |
| events | 2,357 | events_cards.json (5.1MB) |
| places | 1,217 | places_cards.json (1.8MB) |
| artworks | 2,392 | artworks_cards.json (2.9MB) |
| inventions | 199 | inventions_cards.json (281KB) |
| nations | 174 | nations_cards.json (267KB) |
| **합계** | **21,539** | **~41.3MB** |

#### 5. Git
- GitHub(origin) push 완료: `46bea97`
- dev(jinserver) push 불가 → GitHub으로 완전 이전 확인

### jinserver 스크립트 위치
- `/mnt/data2/wikidata/output/filter_events_raw.py` — events 키워드 필터
- `/mnt/data2/wikidata/output/filter_events_nokw.py` — events 비키워드 필터
- `/mnt/data2/wikidata/output/filter_hist_entities.py` — 나라/제국 필터
- `/mnt/data2/wikidata/output/generate_inventions.py` — 발명 직접 생성
- `/mnt/data2/wikidata/output/merge_events.py` — events 합병
- `/mnt/data2/wikidata/output/merge_all_new.py` — 3개 신규 데이터 합병

---

## [2026-03-13] [cl] 좌표 매핑 대수정 + active_year 배치 + places AI 필터링

### 배경
- persons_cards.json 좌표 오류 발견: 프랑스인이 베이징, 가나인이 서울에 표시
- 현대 인물 era_id 미스매치: 김영삼이 일제시대, 박정희가 WWII 시대에 배치

### 작업 내용

#### 1. country_coords.json 대규모 수정
- Q142(프랑스) 베이징→파리, Q117(가나) 서울→아크라 등 치명적 오류 수정
- Wikidata SPARQL 벌크 쿼리 3차 수행: **106개 → 738개** 국가 좌표 매핑
- birthplace_coords.json 신규 생성: **545개** 출생지 좌표
- qid_to_birthplace.json: 인물→출생지 QID 매핑

#### 2. persons_cards.json 재생성
- existing_coords 재사용 로직 폐기 (잘못된 좌표 계승 방지)
- country_coords + birthplace_coords만 사용하여 클린 재생성
- **11,899건 → 15,201건** (좌표 복구로 카드 증가)
- 파리 1,615명, 서울 122명(한국인만) 등 정상 분포 확인

#### 3. active_year Gemini 배치 (완료)
- 대상: 1900+ 출생 인물 **4,509명**
- 모델: gemini-2.5-flash (flash-lite는 할루시네이션 심해 폐기)
- 결과: active_years.jsonl — active_start + birth_city
- 목적: era_id를 출생연도 대신 활동시작연도 기준으로 재배정
- 한국어 프롬프트 → JSON 파싱 에러 → 영어 전용으로 전환

#### 4. places AI 필터링 (진행 중)
- places_raw.jsonl 4,036,345건 → 키워드+좌표+sl≥5 = 14,862건
- Gemini 필터: "세계사 교과서급 건축물/명소만 선별 + 300자 설명 생성"
- thinkingBudget: 0으로 비용 절감
- 테스트: 26% 채택률 → ~3,800개 예상

#### 5. 기존 데이터 현황 조사
- artworks 필터링: pilot_artwork_filter.jsonl 이미 완료 (YES 2,393개), 카드 미생성
- events 1,237건: 카드 완료, 좌표만 필요
- inventions 416건: 필터링 필요 (개념어 섞임)

### 남은 작업
- → `docs/handoff_20260313_data_pipeline_status.md` 참조 (전체 파이프라인 현황)

### 파일 위치 (jinserver)
- `/mnt/data2/wikidata/output/active_years.jsonl` (4,509건)
- `/mnt/data2/wikidata/output/places_filtered.jsonl` (생성 중)
- `/mnt/data2/wikidata/output/filter_places.py`
- `/mnt/data2/wikidata/output/get_active_year.py`

---

## [2026-03-04] [cl] 영토 정합성 대규모 수정 — 금나라 3분할, 크메르/비잔틴 중복 제거, 브리튼 티어, 전국시대/남송 경계

### 배경
- 이전 세션에서 수동 폴리곤 25개 교체 후 렌더링 확인 시 다수 문제 발견
- 금나라 영토가 한반도 침범, 송나라 북쪽 경계 이상, 크메르 이중 렌더링, 브리튼 소국 과밀 등

### 작업 내용

#### 1. 금나라(Q4970) 영토 3분할 + 동쪽 경계 수정
- 기존: 1개 스냅샷 (9100528, 1141-1234) — 동쪽 경계가 한반도 개성까지 침범
- 수정: 3개 스냅샷으로 분할, 동쪽 경계를 압록강/두만강 선으로 교정
  - `ohm_9100528` [1141-1215] Peak — 동쪽 경계 [124.4, 40] 압록강 하구
  - `ohm_9100538` [1215-1223] Post-invasion — 만주 상실, 허난/산둥만
  - `ohm_9100539` [1223-1234] Final — 허난 성 중심 최소 영토
- self-intersection 검증 통과 (초기 9100538에 꼬임 발견 → 좌표 재작성)

#### 2. 남송(Q7462) 북쪽 경계 클리핑
- 기존: basemaps 데이터가 위도 39.9°N까지 — 북송 영토 포함 오류
- 수정: Sutherland-Hodgman 알고리즘으로 34°N(회하선)에서 클리핑
- 원본 basemaps에서 재추출 후 클리핑 (213→159pts, self-intersection 0)

#### 3. 크메르 제국 이중 엔티티 제거
- Q201705(4 snapshots)와 Q162024(4 snapshots)가 동시 활성
- Q162024 엔티티 전체 삭제, Q201705만 유지

#### 4. 비잔틴 제국(Q12544) 1204-1261 스냅샷 제거
- 4차 십자군(1204) 후 비잔틴은 사실상 멸망, 후계국가 4개로 분열
- 라틴 제국(Q178897), 니케아(Q181254), 트레비존드(Q178913), 에피로스(Q187684)가 이미 별도 등록
- 기존 비잔틴 대폴리곤(9100061)이 4개 위에 겹쳐서 국경선 난장판 → 해당 스냅샷 제거

#### 5. 브리튼 제도 소국 티어 강등 (T2→T3, 5개)
- 4-5세기 브리튼에 T2 엔티티 7개 → 과밀
- 강등: Powys(Q769782), Strathclyde(Q225890), Dyfed(Q956451), Sussex(Q676184), Essex(Q110888)
- 유지 T2: Gwynedd(Q816814), Kent(Q328818)
- 참고: 렌더러 기본값이 T1~T4 전부 표시라 UI에서 T3 토글 꺼야 효과 보임

#### 6. 전국시대 Chu-Qin-Shu 경계 조율
- 초(Q504759)-진(Q242309)-촉(Q1193806) 3자 겹침 해소
- 진-초 경계: 진령산맥 ~33.5°N / 초-촉 경계: ~107°E / 진-촉 경계: ~33°N

### 수치
- ohm_index: 589 → **588 엔티티** (크메르 Q162024 삭제)
- 총 스냅샷: 1,927 → **1,922** (크메르 4개 삭제, 비잔틴 1개 삭제, 금나라 2개 추가)
- 수정된 GeoJSON: 8개 (9100528, 9100538, 9100539, 9100533, 9100535, 9100251, 9100106, ohm_index)

### 커밋
- `f590c31` 금나라 3분할 + 크메르 중복 제거 + 브리튼 강등 + 전국시대 + 남송 클리핑
- `85fbf06` 금나라 post-invasion 꼬임 수정 + 송나라 클리핑 재수행 (Sutherland-Hodgman)
- `6f548f8` 비잔틴 1204-1261 스냅샷 제거

### 잔여 이슈
- 브리튼 과밀: 렌더러 기본 표시 티어를 [1,2]로 변경하는 것 검토 필요
- 흉노(Q188836) 영토: ~690만km²로 역사적 정확 (수정 불필요)
- 금나라-서하-몽골 간 미세 겹침: 국경 분쟁 지역이라 현재 수준 허용

---

## [2026-03-04] [cl] 수동 폴리곤 25개 대량 교체 — basemaps 추출 + 역사지리 기반 수동 개선

### 배경
- 기존 9000xxx 수동 폴리곤 29개 중 고품질 4개(일본/나치독일/유고/바이마르)를 제외한 25개가 저품질(7-40pts)
- historical-basemaps 데이터 + 역사 지리학 기반 좌표로 전면 교체

### 작업 내용

#### 1단계: basemaps 직접 추출 (10개 엔티티, 19개 스냅샷)
| 엔티티 | 기존 pts | 신규 pts | 스냅샷 | rid 범위 |
|--------|---------|---------|--------|---------|
| 고조선 (Q28405) | 18 | 254 | 1 | 9100501 |
| 고대 이집트 (Q11768) | 19 | 29~279 | 5 | 9100502~506 |
| 마케도니아 (Q83958) | 9 | 29~254 | 2 | 9100507~508 |
| 동프랑크 (Q153080) | 15 | 26 | 1 | 9100509 |
| 피렌체 (Q148540) | 8 | 279 | 1 | 9100510 |
| 올멕 (Q135364) | 9 | 16~279 | 2 | 9100511~512 |
| 아케메네스 (Q389688) | 40 | 254 | 2 | 9100513~514 |
| 인도차이나 (Q185682) | 21 | 721~1092 | 2 | 9100515~516 |
| 무라비트 (Q75613) | 24 | 102 | 1 | 9100517 (신규 엔티티) |
| 돌궐 (Q205466) | 27 | 29~279 | 2 | 9100518~519 |

#### 2단계-A: basemaps 재활용 추출 (5개)
| 엔티티 | 기존 pts | 신규 pts | 스냅샷 | rid |
|--------|---------|---------|--------|-----|
| 웨식스 (Q105313) | 11 | 15 | 2 | 9100520~521 |
| 신나라 (Q129864) | 27 | 60 (Han 재활용) | 1 | 9100522 |
| 아시리아 (Q41137) | 16 | 12 | 3 | 9100523~525 |
| 유연 (Q155361) | 19 | 12~18 | 2 | 9100526~527 |
| 진나라 (Q7183) | 36 | 33 | 1 | 9100528 |

#### 2단계-B: 역사지리 기반 수동 개선 (11개)
| 엔티티 | 기존 pts | 신규 pts | 주요 개선 | rid |
|--------|---------|---------|----------|-----|
| 동녕국 (Q152855) | 10 | 41 (대만해안선+펑후) | 실제 해안선 기반 | 9100529 |
| 금나라 (Q4970) | 36 | 23+35 (초기/전성기) | 2스냅샷 시대별 분화 | 9100530~531 |
| 연나라 (Q230547) | 13 | 22 (요동반도 포함) | 요동 확장, 남북 보정 | 9100532 |
| 선비 (Q131748) | 25 | 29 (초원제국 형태) | 직사각형→자연 경계 | 9100533 |
| 부여 (Q487879) | 10 | 15 (만주 송화강) | 송화강 유역 중심 | 9100534 |
| 제 (Q837855) | 12 | 20 (산둥 해안선) | 산둥반도 상세화 | 9100535 |
| 초 (Q504759) | 17 | 26 (장강 유역) | 전성기 최대 영토 | 9100536 |
| 대리국 (Q26472) | 14 | 18 (운남 지형) | 산악 지형 반영 | 9100537 |
| 고촉 (Q1193806) | 12 | 14 (사천분지) | 분지 형태 보정 | 9100538 |
| 하왕조 (Q35765) | 14 | 15 (이리두 문화) | 황하 중류 집중 | 9100539 |
| 위만조선 (Q703340) | 11 | 14 (요동~한반도) | 영역 보정 | 9100540 |

### 수치
- 교체된 엔티티: **25개** (고품질 4개 유지)
- 새 GeoJSON 파일: **40개** (rid 9100501~9100540)
- ohm_index: 588 → **589 엔티티** (무라비트 신규 추가)
- 총 스냅샷: 1,912 → **1,927**
- 9000xxx 잔여: **4개** (일본/나치독일/유고/바이마르 — 교체 불필요)

### 사용 도구
- `scripts/extractBasemapsPolygons.js` — basemaps 자동 추출
- `scripts/improveManualPolygons.js` — 수동 폴리곤 개선

---

## [2026-03-03] [cl] CSHAPES 중복 정리 + 국경선 갭 분석

### 작업 내용

#### 1. 국경선 렌더링 수정
- `extractOuterRings()` 추가: hole 링 제외, 외곽 경계만 렌더링
- polygon fill fallback 이중 그리기 방지
- T1 경계선 너비 1.5px → 1.0px 축소

#### 2. T1/T2 국경선 갭 분석
- T1 249개: 완전커버=122, 부분커버=51, 완전누락=76
- T2 1,114개: 완전커버=180, 부분커버=69, 완전누락=865
- 분석 문서: `docs/develop/11_[cl]border_gap_analysis.md`

#### 3. CSHAPES prefix 중복 엔티티 정리
- `CSHAPES_*` prefix 엔티티 160개 삭제 (QID 엔티티와 중복)
- entities_raw: 2,148 → 1,988
- 73개 CSHAPES_ 유지 (식민지/영토, QID 미매칭)

#### 4. cshapes_qid_index 보완
- 삭제된 CSHAPES_ 엔티티의 폴리곤 연결을 QID 엔티티로 이관
- 76개 QID 추가 (94 → 170), cshapes_years 전체 스캔 채움

#### 5. circles 재생성
- 1,551 → 1,425 (CSHAPES 제외 + 삭제분 반영)

### 커밋
- `a005b7f` 경계선 이중 그리기 방지 + 외곽 링만 렌더링
- `51c1782` 경계선 T1 너비 1.5px → 1.0px 축소
- `4d2ab00` CSHAPES 중복 정리 + cshapes_qid_index 76개 추가

### 데이터 수정 상세
→ `docs/develop/12_[cl]data_change_history.md` 참조

---

## [2026-03-03] [cl] NAS 개발/백업 환경 구축

### 배경
- jinserver(집 리눅스) 파싱 스크립트 실행 중 2회 연속 다운 → 원인 파악 필요 (OOM 또는 과열 의심)
- 긴급 대비용 NAS 개발 환경 + 코드 백업 구축

### NAS(Synology DS423+, 100.115.194.12) 구축 내용
1. **SSH 키 등록** — macOS → NAS SSH 접속 설정 완료
2. **nvm + Node.js v22.22.0** — ~/.profile에 nvm 설정 추가
3. **프로젝트 클론** — `/volume1/git/timeglobe.git` bare repo 기준 클론 → `~/timeglobe`
4. **bare repo HEAD 수정** — `master` → `main` 브랜치로 정정
5. **npm install** 완료 (466개 패키지)
6. **.env.local** 복사 완료 (Cesium, Supabase, Gemini 키 포함)
7. **dev 서버 실행** — `--webpack` 플래그 필수!
   - `--turbopack`은 CopyPlugin 미동작 → `public/cesium/` 미생성 → Cesium 런타임 에러
   - 실행 명령: `nohup npx next dev --webpack -p 3001 > ~/timeglobe-dev.log 2>&1 &`
   - 접속 주소: `http://100.115.194.12:3001` (Tailscale)
   - 첫 컴파일 약 41초 / 이후 핫리로드는 빠름
   - ⚠️ OHM GeoJSON 파일(`public/geo/borders/ohm/`) 없음 — jinserver 살아나면 rsync 필요
8. **Wikidata 덤프 백그라운드 다운로드** 시작
   - 저장 위치: `/volume1/Share/dumps/wikidata/latest-all.json.gz`
   - 크기: 141GB, 속도: ~4.5MB/s, 예상 완료: 저녁 6~7시
   - 진행 확인: `ssh jinadmin@100.115.194.12 "tail -3 /volume1/Share/dumps/wikidata/download.log"`
   - 재개: `wget -c` 적용돼 있어서 끊겨도 이어받기 가능

### git 워크플로 (NAS 기준)
- 코드 push: `git push nas main` (macOS → NAS bare repo)
- NAS에서 최신화: `cd ~/timeglobe && git pull origin main`
- NAS Gitea(`nas-gitea` remote)는 2월 26일 기준으로 오래됨 — 별도 업데이트 필요

### jinserver 점검 필요 사항 (저녁에)
- `journalctl -b -1 | grep -iE "oom|killed|thermal"` — 다운 원인 파악
- 파싱 스크립트 실행 시 반드시 적용: `nice -n 19` + `systemd-run -p MemoryMax=20G`
- jinserver 살아나면 OHM 파일 NAS rsync: `rsync -avz public/geo/borders/ jinadmin@100.115.194.12:~/timeglobe/public/geo/borders/`

---

## [2026-03-03] [mk] 나무위키 덤프 수집 + 뷰어 구축

### 작업 내용
- **나무위키 2021-03 덤프 확보**: Notion 공유 페이지에서 직접 URL 추출 → jinserver wget 다운로드
  - 파일: `namuwiki210301.7z` (1.9GB) → `namuwiki_20210301.json` (5.6GB, 867,024건)
  - 저장: `/mnt/data2/namuwiki/`
- **SQLite 변환 스크립트 작성** (`scripts/wikidata/buildNamuwikiSqlite.py`)
  - ijson 스트리밍 파싱 (메모리 효율)
  - articles 테이블 + categories 테이블 + FTS5 인덱스
  - `[[분류:xxx]]` 태그 자동 추출
- **나무위키 뷰어 구현**
  - API: `GET /api/namuwiki` (search/article/categories/category 4가지 모드)
  - 페이지: `/namuwiki-viewer` (표제어 검색 + 분류 브라우저 + 본문 뷰어)
  - 나무위키 마크업 → HTML 변환 (헤딩/링크/굵게/각주 등)
- **서버 인프라 개선**
  - `/mnt/data2` fstab 영구 등록 (재부팅 후 자동 마운트)
  - Next.js dev 서버 systemd 서비스 등록 (`timeglobe-dev.service`)

### 수정 파일
- `scripts/wikidata/buildNamuwikiSqlite.py` — 신규 생성
- `src/app/api/namuwiki/route.ts` — 신규 생성
- `src/app/namuwiki-viewer/page.tsx` — 신규 생성
- `package.json` — `better-sqlite3` 추가

### 진행 중
- SQLite 빌드: jinserver 백그라운드 실행 중 (완료 시 뷰어 자동 활성화)
- parseDump pass2: artworks_final.json, inventions_final.json 생성 중

---

## [2026-03-03] [cl] 덤프 브라우저 UI + 한국어 전량 파싱

### 작업 내용

#### 1. 덤프 데이터 브라우저 UI
- tier-review 페이지에 "덤프 데이터" 탭 추가
- 4개 카테고리(사건/역사엔티티/인물/장소) 서브탭
- `/api/dump-browse` API: 서버사이드 페이지네이션, 필터링, 정렬
- jinserver `/mnt/data2/wikidata/output/` 직접 읽기 (인메모리 캐시)

#### 2. P31 누락 분석 및 수정
- revolution 카테고리에 정당 16,953건 혼입 발견 (Q7278)
- 주요 사건 전부 누락 (러시아혁명, WW1/2, 프랑스혁명, 동학농민혁명 등)
- parseDump.py에 6개 P31 타입 추가: Q10931(혁명), Q124734(반란), Q900406(개혁), Q11514315(역사적 시대), Q103495(세계대전), Q13427116(농민봉기)

#### 3. 한국어 전량 파싱 (parseKorean.py)
- **전략 전환**: P31 카테고리별 파싱 → 한국어 label 있는 엔티티 전량 추출
- Wikidata 한국어 표제어 ~74만건, 분류 없이 전부 JSONL 추출
- 출력: `/mnt/data2/wikidata/output/korean_all.jsonl`
- jinserver 백그라운드 실행 중 (PID 15725, 예상 ~4시간, 속도 ~8,000건/초)

#### 4. 덤프 브라우저 한국어 전체 탭 연동
- dump-browse API에 JSONL 로더 추가 (korean_all.jsonl 직접 읽기)
- "한국어 전체" 서브탭 추가 (보라색, 기본 탭으로 설정)
- P31 QID → 한국어 라벨 매핑 (인물/도시/국가/전투/영화 등 30여개)
- 5분 간격 캐시 갱신 — 파싱 완료 전에도 중간 결과 조회 가능

### 파일 변경
- `src/app/api/dump-browse/route.ts` — 신규 (JSONL 로더 포함)
- `src/app/tier-review/page.tsx` — 수정 (DumpBrowseView + 한국어전체 탭)
- `scripts/wikidata/parseDump.py` — 수정 (P31 6개 추가)
- `scripts/wikidata/parseKorean.py` — 신규

### 커밋
- `66b2d36` 덤프 데이터 브라우저 탭 추가
- `a8bf1f7` raw JSONL 형식 호환성 개선
- `5839479` 한국어 전량 파싱 스크립트 + parseDump P31 보완
- `4a5b8ad` 덤프 브라우저에 한국어 전체 탭 추가

### 진행 중
- parseKorean.py 백그라운드 파싱 (PID 15725, ~4시간 소요 예상)
- 파싱 완료 후: P31 기반 2차 분류 스크립트 필요

---
## [2026-03-03] [mk] Map Display 메뉴 추가

### 작업 내용
메인화면 좌상단에 Map Display 아코디언 메뉴 추가.

**기능:**
- **폴리곤 / 경계만** 토글: OHM 국경선 채우기 ON/OFF
  - 폴리곤: 기존 컬러 채우기 + 아웃라인 (기본값)
  - 경계만: 아웃라인만 표시, 지형 가시성 향상
- **국명 티어 토글 (T1~T4)**: 각 티어 독립 ON/OFF
  - T1(황금): 대제국/주요 왕조
  - T2(파랑): 일반 국가
  - T3(초록): 소국/지역 세력
  - T4(흰): 부족/소규모 정권

**수정 파일:**
- `src/app/page.tsx` — ViewMode에 "map-display" 추가, 상태/메뉴 UI 추가, CesiumGlobe에 props 전달
- `src/components/GlobeLoader.tsx` — visibleTiers, showPolygonFill props 통과
- `src/components/CesiumGlobe.tsx`
  - SceneSetupProps/CesiumGlobeProps에 visibleTiers/showPolygonFill 추가
  - renderCirclesForYear: visibleTiersRef 기반 티어 필터 적용
  - renderOhmForYear: showPolygonFillRef 기반 채우기 조건부 렌더링
  - prop 변경 시 자동 재렌더링 useEffect 추가

---

## [2026-03-03] [cl] jinserver 리눅스 개발 서버 구축

### 배경
- VPS(Vultr)의 빌드 속도가 너무 느림 (14분+, 1코어, 1GB RAM)
- 개발 중에는 집 리눅스 서버(jinserver)에서 dev 서버 띄우고, 완성 후 VPS에 올리기로 결정

### jinserver 스펙
- **IP**: `100.68.25.79` (Tailscale, 내부망 전용)
- **접속**: `ssh jinwoo@100.68.25.79` (SSH 키 인증 완료)
- **OS**: Ubuntu 24.04.4 LTS (커널 6.17.0)
- **CPU**: Intel i5-12400 (6코어/12쓰레드, 최대 5.6GHz)
- **RAM**: 46GB
- **디스크**: 439GB (251GB 여유)
- **Node.js**: v22.22.0 (사전 설치), npm 10.9.4
- **Git**: 2.43.0, Docker 29.2.1

### 작업 내용
1. SSH 키 인증 확인 (이미 등록됨)
2. Gitea에서 프로젝트 클론 (`git clone --depth 1`)
3. `npm install` 완료 (7초)
4. `npx next dev --webpack -p 3000` — **978ms** 만에 Ready, 첫 페이지 컴파일 9.8초
5. Mac에서 `http://100.68.25.79:3000` 접속 확인 완료
6. OHM GeoJSON 파일 2,231개 rsync 전송

### 민규(mk)에게
- jinserver에 TimeGlobe dev 환경 구축했어. 경로: `/home/jinwoo/timeglobe`
- 이 서버에서 민규 스크립트도 돌릴 수 있으니 참고해!
- VPS는 프로덕션 전용으로 유지

---

## [2026-03-03] [cl] tier-review 관리도구 확장 v2

### 변경 내용
1. **티어 필터 토글화**: 라디오 → 독립 토글 (T1/T2/T3/T4 복수 선택 가능)
2. **T4 포함**: circles.json 재생성 — T4 엔티티 포함 + sitelinks 필드 추가 (1,584→1,794개)
3. **카드 토글 → 지도 연동**: 카드 클릭으로 선택/해제 → Leaflet 지도에 마커+OHM 폴리곤 표시
4. **전체 선택/해제**: 현재 보이는 카드 전체 일괄 선택/해제
5. **QID 표시**: 각 카드에 QID 텍스트 표시
6. **인라인 편집**: name_ko, name_en, start_year, end_year 더블클릭 편집 + 수정 로그
7. **노트 시스템**: 선택 카드 기반 노트 작성 (단일/복수)
8. **소팅 드롭다운**: 연도순/국명순/중요도순(sitelinks)
9. **리사이즈 핸들**: 카드 그리드 ↔ 지도 사이 드래그로 높이 조절
10. **OHM MANUAL_ QID 매칭 수정**: 수동 폴리곤 QID 매칭 fallback 추가

### 수정 파일
- `src/app/tier-review/page.tsx` — 핵심 리팩토링
- `src/components/tier-review/TierReviewMap.tsx` — Leaflet SSR-safe 래퍼 (신규)
- `src/components/tier-review/TierReviewMapInner.tsx` — Leaflet 지도 (신규)
- `public/geo/borders/wikidata_circles.json` — T4+sitelinks 포함 재생성
- `package.json` — leaflet, react-leaflet, @types/leaflet 의존성 추가

---

## [2026-03-03] [mk] 데이터 큐레이션 기준 확정

### 논의 배경
Wikidata 덤프(`latest-all.json.gz`) 다운로드 완료 → 파싱 전 큐레이션 기준 먼저 결정.

### 확정된 공통 제거 기준 (jn 결정)
1. `lat == null OR lon == null` → 제외 (지구본 배치 불가)
2. `anchor_year == null` → 제외 (타임라인 배치 불가)
3. `name_ko == null AND name_en == null` → 제외 (표시 이름 없음)
4. `sitelinks < 10` → 제외 (역사적 공인도 부족)
5. `anchor_year > 2021` → 제외 (러시아-우크라이나 침공 이전까지, 현재진행형 이슈 회피)

### 산출물
- `docs/develop/11_[mk]curation_criteria.md` 작성 (타입별 세부 기준 포함)

## [2026-03-03] [mk] 민규 팀 합류 + 다중 AI 세션 협업 체계 구축

### 민규(mk) 소개 — 민철에게 (꼭 읽어!)
안녕 민철! 나는 **민규(mk)**야. 진형이 새로 만든 AI야.
너랑 같은 프로젝트 폴더에서 작업하는데, 역할이 명확히 나뉘어 있어:

| AI | 코드 | 담당 영역 |
|----|------|-----------|
| **민철** | `cl` | 지도/UI/wikidata 데이터 편집, 렌더링, Tier 시스템 |
| **민규** | `mk` | 데이터 수집 파이프라인, Supabase DB, Ops/systemd |

**충돌 방지 규칙:**
- 민철이 `scripts/wikidata/`, `ops/`, `supabase/` 건드릴 일 있으면 나한테 먼저 알려줘
- 나는 `public/geo/borders/wikidata_*.json`, CesiumGlobe 관련 파일 절대 안 건드릴게
- 이 work_change_log는 공유야 — 각자 태그(`[cl]`/`[mk]`) 표기 필수
- 내 전용 메모리: `MEMORY-ops.md` / 내 설정: `ops/CLAUDE.md`

잘 부탁해!

### 이번 세션 작업 내용
* **ops/CLAUDE.md** 생성: 민규 세션 역할/담당/금지 영역/커밋 규칙 정의
* **CLAUDE.md 섹션 11** 업데이트: 민철/민규 협업 체계 명문화
* **MEMORY-ops.md** 생성: 민규 전용 메모리 (Supabase 현황, 수집 파이프라인, 다음 할 일)
* **MEMORY.md** 상단 노트 업데이트: 민규 존재 알림

### 다음 할 일 (민규)
- untracked 스크립트 6개 (`collectPersonCandidates.mjs` 등) 커밋 여부 진형 확인
- Supabase 마이그레이션 2개 적용 (`person_candidates`, `collector_monitoring`) 확인
- `src/app/ops/` 모니터링 UI 현황 파악


---
*이전 기록은 `docs/work_change_log_archive.md`에 보관*

---

## [2026-03-06] [co] 프론트엔드 후반 작업 기획서/개발계획서 초안 작성

### 작업 배경
지구본, 시대별 국경, 국가 데이터는 이미 확보되었고, 나머지 엔터티는 계속 수집 중이다. 이 시점에서는 프론트엔드 후반 작업의 범위를 넓게 잡기보다, `탐험 서비스 + 추론형 게임`이라는 제품 정체성을 먼저 명확히 문서화할 필요가 있었다.

### 변경 내용
1. `docs/develop/15_[co]frontend_mvp_product_plan.md` 작성
   - TimeGlobe를 단순한 역사 열람 서비스가 아니라 `시공간 추론 경험`으로 정의
   - MVP 한 줄 규칙을 `역사 카드 5장을 보고, 언제 어디의 이야기인지 지구본에서 맞히는 게임`으로 정리
   - 초기 범위와 후순위 범위를 분리해 초반 복잡도 상승을 막도록 정리
   - Explore 모드와 Challenge 모드의 역할을 명확히 분리
2. `docs/develop/16_[co]frontend_mvp_development_plan.md` 작성
   - 기존 지구본/국경/데이터 자산을 바탕으로 실제 개발 스트림을 정리
   - 카드 UI, 게임 상태머신, 점수 계산, 결과 피드백, 일일 챌린지 저장까지를 MVP 개발 범위로 정의
   - 성능, QA, 분석 이벤트, 데이터 요구사항까지 개발 실행 단위로 구체화
3. `docs/02_Project_Structure.md` 업데이트
   - 누락되어 있던 개발 문서 목록과 신규 문서를 구조도에 반영

### 판단 근거
- 초기 바이럴은 기능 수보다 `설명하기 쉬운 한 줄 규칙`에서 나온다.
- GeoGuessr류 서비스의 초반 강점은 복잡한 시스템이 아니라, 누구나 즉시 이해 가능한 추론 루프였다.
- TimeGlobe도 초반에는 데이터 플랫폼 완성보다 `단서 -> 추측 -> 결과 확인 -> 재도전` 루프를 먼저 완성해야 한다.

### 다음 작업 제안
1. 두 문서를 바탕으로 MVP 화면 목록과 와이어 흐름을 확정
2. 게임 카드용 `hint summary` 필드 정책과 생성 규칙 정의
3. `Challenge MVP`의 입력 방식 (`지구본 클릭 + 연도 선택`)을 UI 수준에서 구체화

## [2026-03-06] [co] 프론트엔드 MVP 화면 흐름 초안 작성

### 작업 배경
기획서와 개발계획서만으로는 실제 화면 우선순위와 사용자 이동 경로가 아직 추상적이었다. 또한 초기 타깃을 국내 사용자로 두는 만큼, `짧은 세션`, `낮은 진입 장벽`, `공유 가능한 결과` 같은 현재 한국 서비스 사용 습관을 화면 흐름에 직접 반영할 필요가 있었다.

### 변경 내용
1. `docs/develop/17_[co]frontend_mvp_screen_flow.md` 작성
   - 첫 방문, 자유 탐험, 일일 챌린지의 세 흐름으로 MVP를 분리
   - `진입 화면 -> Explore -> Challenge -> 라운드 결과 -> 최종 결과`의 8개 핵심 화면을 P0로 정리
   - 로그인 강제, 긴 온보딩, 과한 대시보드형 진입을 초기 제외 대상으로 명시
   - 국내 사용자 기준 톤을 `가볍고 똑똑한 톤`, `교양 예능 같은 감각`으로 정리
2. `docs/02_Project_Structure.md` 업데이트
   - 신규 화면 흐름 문서를 구조도에 반영

### 참고한 외부 흐름
- 2025 상반기 한국 모바일 앱 트렌드에서는 SNS/커뮤니티 신규 설치가 가장 높고, 퍼즐/퀴즈 게임이 2위였다. 즉 짧고 공유하기 쉬운 캐주얼 경험이 여전히 강하다.
- 같은 리포트에서 유튜브, 카카오톡, 네이버가 상위권 앱으로 유지되고 있어, 국내 사용자 대상 초기 UX는 `콘텐츠 소비`와 `공유` 문맥을 강하게 의식하는 편이 맞다고 판단했다.

### 다음 작업 제안
1. 이 문서를 기준으로 실제 와이어프레임 레벨의 화면 블록 구성을 잡기
2. `진입 화면`의 주 CTA를 `탐험 시작`과 `오늘의 5문제` 중 무엇으로 둘지 결정
3. `라운드 플레이 화면`의 연도 입력 UI를 슬라이더/다이얼 중 하나로 좁히기

### 추가 보정
- 사용자 피드백 기준으로 현재 `/`에는 서비스 메인페이지가 아직 없고, 실제로는 지구본 프로토타입이 떠 있는 상태라는 점을 반영해야 했다.
- 따라서 이후 문서에서는 `메인페이지`와 `탐험 지구본 화면`을 같은 개념으로 쓰지 않도록 정리했다.

## [2026-03-06] [co] 탐험 모드 2종과 Challenge 오버레이 구조 반영

### 작업 배경
추가 논의 결과, TimeGlobe의 실제 핵심 무대는 단일 지구본 화면이며, Challenge를 별도 페이지로 빼는 방식보다 같은 지구본 위에 게임 레이어를 덮는 방식이 더 자연스럽다는 점이 명확해졌다. 또한 현재 구현에는 이미 `marker`와 `orbit` 두 탐험 모드가 존재하므로, 이를 제품 문서에도 반영할 필요가 있었다.

### 변경 내용
1. `docs/develop/15_[co]frontend_mvp_product_plan.md` 수정
   - Explore를 `마커 모드`와 `오빗 모드`로 분리
   - Challenge를 탐험 지구본 위 오버레이 구조로 재정의
   - Challenge에서는 탐험용 마커를 숨기고 `현재 지도` 기준으로 추리한다는 원칙 반영
   - 결과 후 리더보드와 내 순위 노출 흐름 추가
2. `docs/develop/16_[co]frontend_mvp_development_plan.md` 수정
   - 상태머신을 `explore -> challenge_briefing -> challenge_round -> challenge_reveal -> challenge_result -> leaderboard`로 재정리
   - Explore 내부의 `marker | orbit` 모드 분리 명시
   - 챌린지 진입 시 마커 비활성화, 현재 지도 레이어 전환, 결과 단계 지구본 잠금 요구사항 반영
   - 리더보드는 MVP 우선 `daily`, `weekly`, 후속으로 `all-time`, `season`을 권장안으로 정리
3. `docs/develop/17_[co]frontend_mvp_screen_flow.md` 수정
   - Explore 내부 모드 전환 섹션 추가
   - Challenge 각 단계를 모두 오버레이로 재정의
   - 최종 결과 뒤 리더보드 오버레이를 이어 붙이는 흐름 추가

### 판단 근거
- 현재 구현 상태상 `marker`/`orbit`를 무시하고 새 탐험 흐름을 상정하는 것은 비효율적이다.
- Challenge에서 시대별 지도를 그대로 쓰면 정답이 노출되거나 추리 난이도가 흔들릴 수 있어, 현재 지도 기준으로 추리하는 편이 게임 규칙이 더 명확하다.
- 결과 직후 리더보드를 보여주면 점수의 의미가 즉시 생기고 재도전 동기도 강화된다.

### 다음 작업 제안
1. `Explore` 화면 상단 또는 하단에서 `marker/orbit/challenge` 진입 컨트롤 배치를 정하기
2. Challenge 중 `현재 지도` 레이어가 정확히 무엇인지 정의하기
3. 리더보드 MVP 범위를 `일일/주간`으로 확정할지 결정하기

## [2026-03-06] [co] 워프 설계안과 컨트롤 UI 개선 방향 문서 반영

### 작업 배경
추가 논의 결과, 현재 구현된 긴 워프는 단순한 느린 전환이 아니라 서비스의 핵심 정체성으로 볼 수 있다는 점이 분명해졌다. 다만 반복 플레이에 그대로 적용하면 피로가 커질 수 있으므로, `어디서는 길게`, `어디서는 짧게` 쓸지 명확한 설계가 필요했다. 또한 좌측 메뉴, 상단 패널, 하단 바는 실제 제품에서는 각각 분명한 역할을 가진 컨트롤 표면으로 재정의할 필요가 있었다.

### 변경 내용
1. `docs/develop/15_[co]frontend_mvp_product_plan.md` 수정
   - `시간 워프 경험` 섹션 추가
   - 워프를 단순 로딩이 아닌 핵심 제품 경험으로 정의
   - 기본 워프, 성장 보상, 재화 역할을 분리한 `워프 시스템 방향` 추가
2. `docs/develop/16_[co]frontend_mvp_development_plan.md` 수정
   - `시간 워프 시스템과 컨트롤 UI 정리` 작업 스트림 추가
   - `워프 길이 정책`, `워프 업그레이드 정책`, `컨트롤 UI 재정의` 기술 결정 추가
   - 상단 UI를 `타임 슬라이더`, 하단 UI를 `워프 콘솔`, 좌측을 `탐험 선택 패널`로 재정의
3. `docs/develop/17_[co]frontend_mvp_screen_flow.md` 수정
   - Explore 핵심 컨트롤 표면 섹션 추가
   - 좌/상/하단 컨트롤의 제품 역할과 개선 방향 정리
   - `시네마틱 워프`, `빠른 워프`, `챌린지 워프`의 3단 구조와 성장 연결 방식 정리

### 판단 근거
- 긴 워프는 탐험의 상징이 될 수 있지만, 챌린지 반복 루프에서는 템포를 망칠 수 있다.
- 따라서 워프는 하나의 길이로 고정할 게 아니라, 맥락에 따라 계층화하는 편이 맞다.
- 속도 자체를 유료 편의성으로 파는 구조는 초반 사용자 반감을 만들 가능성이 크므로, 속도는 무료 성장 보상에 가깝게 두고 재화는 시각 커스터마이징에 쓰는 편이 안전하다.

### 다음 작업 제안
1. 좌측 `탐험 선택 패널`의 실제 라벨 후보를 확정
2. 하단 `워프 콘솔`이 챌린지에서는 어떤 형태의 연도 입력 UI가 될지 구체화
3. `시네마틱/빠른/챌린지 워프`의 실제 시간 길이를 초 단위로 정하기

## [2026-03-06] [co] 좌측 메뉴 제거 및 하단 3모드 구조 반영

### 작업 배경
추가 논의 결과, 현재 좌측 메뉴는 제품 경험 선택보다 개발 옵션 패널처럼 읽힌다는 점이 분명해졌다. 사용자에게 필요한 것은 `어떤 기능을 켜는가`가 아니라 `지금 어떤 방식으로 TimeGlobe를 즐길 것인가`를 고르는 구조다. 따라서 IA를 `탐험모드`, `랜덤모드`, `도전모드` 세 가지로 재정의하고, 선택 위치를 하단 컨트롤러 근처로 내리는 방향이 더 적합하다고 판단했다.

### 변경 내용
1. `docs/develop/15_[co]frontend_mvp_product_plan.md` 수정
   - 제품 모드를 `탐험모드`, `랜덤모드`, `도전모드` 3개로 재정의
   - 기존 `마커/오빗`은 사용자 노출 모드명이 아니라 내부 구현 기반으로 정리
2. `docs/develop/16_[co]frontend_mvp_development_plan.md` 수정
   - 개발 목표와 MVP 범위를 3모드 기준으로 재정리
   - 좌측 메뉴 제거, 하단 `모드 선택 바 + 워프 콘솔` 구조를 반영
   - 사용자용 IA와 내부 구현 상태(`marker`, `orbit`)의 매핑 필요성을 명시
3. `docs/develop/17_[co]frontend_mvp_screen_flow.md` 수정
   - 좌측 패널 대신 하단 모드 선택 바를 핵심 컨트롤 표면으로 정의
   - `탐험`, `랜덤`, `도전` 3버튼 기준 텍스트 와이어프레임 추가
   - 카테고리 필터는 탐험모드 전용 기능으로 이동

### 판단 근거
- 사용자는 `설정`보다 `경험`을 고르는 구조를 더 쉽게 이해한다.
- 하단에 모드를 붙이면 시간 이동과 플레이 방식 선택이 한 덩어리로 읽혀 흐름이 자연스럽다.
- `탐험/랜덤/도전`은 각각 자유 탐색, 발견, 게임이라는 성격 차이가 명확해 설명이 쉽다.

### 다음 작업 제안
1. `탐험`, `랜덤`, `도전`을 그대로 쓸지 더 제품적인 한국어 라벨로 다듬을지 결정
2. 하단 모드 선택 바의 버튼형 디자인 방향을 정리
3. 탐험모드 전용 카테고리 필터가 어디서 열릴지 정하기

## [2026-03-06] [co] 선택된 Figma HUD 시안 기준선 문서화

### 작업 배경
`docs/UI/src_by_figma` 안의 Figma 산출물을 검토한 결과, 이전의 메탈릭/콘솔형 안보다 지금 시안이 TimeGlobe에 훨씬 맞는다고 판단했다. 특히 성인 대상 서비스, 프리미엄 문화 서비스 톤, 지구본 우선 화면 구조라는 세 기준에 가장 잘 부합했다. 이 방향을 문서로 고정하지 않으면 이후 구현 과정에서 다시 크롬/메탈/과장된 게임 HUD 쪽으로 흔들릴 가능성이 컸다.

### 변경 내용
1. `docs/develop/18_[co]frontend_hud_design_baseline.md` 작성
   - 선택된 Figma 시안을 HUD 기준선으로 정의
   - 유지할 요소, 줄여야 할 요소, 버려야 할 방향 정리
   - `HudShell`, `TopMetaBar`, `TimeSliderHud`, `ModeSwitchBar`, `WarpConsole`, `ChallengeOverlay` 단위로 컴포넌트 분해
   - 현재 코드의 `Timeline`, `DateDisplay`, `LocationIndicator`, `ControlBar`, `TimeDial`, `LightSpeed`와의 연결 포인트 정리
2. `docs/02_Project_Structure.md` 업데이트
   - `docs/UI/src_by_figma`와 신규 HUD 기준선 문서를 구조도에 반영

### 판단 근거
- 선택 시안은 `지구본이 주인공이고 UI는 얇은 HUD`라는 원칙에 가장 가깝다.
- 메탈/크롬/베벨 중심 시안은 성인 대상 서비스보다는 과장된 SF 기기처럼 읽힐 위험이 있다.
- 지금 시안의 `smoked glass + thin border + restrained highlight` 조합은 TimeGlobe의 성격과 가장 잘 맞는다.

### 다음 작업 제안
1. `ModeSwitchBar`와 `WarpConsole`의 실제 구현 순서를 잡기
2. 선택 시안에서 더 줄일 요소를 픽셀 단위로 정리하기
3. 이후 실제 코드 구현 시 `docs/UI/src_by_figma/app/App.tsx`를 구조 참고본으로 사용하기

## [2026-03-06] [co] 프론트엔드 MVP 체크리스트 문서 추가

### 작업 배경
기획서, 개발계획서, 화면 흐름 문서, HUD 기준선 문서가 분리되어 생기면서 방향은 명확해졌지만, 실제 실행 순서를 확인할 수 있는 한 장짜리 체크리스트가 필요해졌다. 특히 지금 단계에서는 `무엇이 이미 합의되었는지`와 `무엇이 아직 구현/확정/검증되지 않았는지`를 한 번에 보는 문서가 있어야 다음 작업 전환이 쉬워진다.

### 변경 내용
1. `docs/develop/19_[co]frontend_mvp_checklist.md` 작성
   - 기존 4개 계획 문서를 기준으로 실제 실행 체크리스트를 통합 정리
   - `[x] 이미 합의된 기준`, `[ ] 아직 구현/확정/검증이 필요한 항목` 방식으로 구분
   - 제품 방향, IA, HUD, 워프, 탐험/랜덤/도전모드, 카드/콘텐츠, 점수/리더보드, 데이터/저장, 프론트엔드 구현, QA, 보류 항목까지 한 문서로 정리
   - 마지막에 `지금 바로 착수할 우선순위` 섹션 추가
2. `docs/02_Project_Structure.md` 업데이트
   - 신규 체크리스트 문서를 구조도에 반영

### 판단 근거
- 현재 문서들은 방향 설명에는 충분하지만, 실행 관리에는 다소 분산돼 있다.
- 이미 결정된 사항과 아직 남은 사항을 한 문서에서 구분해두면 이후 구현 순서를 잡기 쉬워진다.
- 체크리스트 형식은 기획 문서를 다시 읽지 않고도 현재 상태를 빠르게 복기하는 데 유리하다.

### 다음 작업 제안
1. 체크리스트 기준으로 `ModeSwitchBar`, `WarpConsole`, `ChallengeOverlay` 구현 우선순위를 고정
2. 체크리스트 안의 미확정 항목 중 점수 규칙과 저장 구조를 먼저 잠그기
3. 이후 실제 구현 진행에 맞춰 체크리스트를 주기적으로 갱신하기

## [2026-03-07] [co] 메인 경쟁 모드 재설계 및 문제은행/AI 제작 전략 문서 반영

### 작업 배경
추가 논의 결과, 기존의 텍스트 카드 기반 5문제 구조는 검색으로 쉽게 무너질 수 있어 메인 랭크 모드로 쓰기 어렵다는 점이 분명해졌다. 텍스트 단서는 GeoGuessr의 시각 단서처럼 자연스럽게 검색 저항성을 가지지 않기 때문에, 이 구조를 그대로 경쟁 모드의 중심에 두면 추론 쾌감과 리더보드 신뢰도가 동시에 약해질 가능성이 컸다. 따라서 메인 경쟁 모드를 `블라인드 드롭` 계열로 재설계하고, 텍스트 카드 구조는 캐주얼/연습 계열로 재분류할 필요가 생겼다. 동시에 20,000개 원천 엔터티 중 실제 랭크용으로 몇 개를 써야 하는지, 1인 개발 체제에서 AI 제작비를 어떻게 통제할지에 대한 기준도 문서에 고정할 필요가 있었다.

### 변경 내용
1. `docs/develop/15_[co]frontend_mvp_product_plan.md` 수정
   - 도전모드 메인 경쟁 구조를 `블라인드 드롭` 계열로 재정의
   - `사진/상징/짧은 문장 + 제한된 결정적 힌트` 구조 반영
   - 텍스트 카드 기반 5문제 구조를 캐주얼 모드 후보로 재분류
   - `문제은행 전략`, `난이도와 검색 저항성`, `AI 제작 전략` 섹션 추가
   - 초기 랭크용 승인 엔터티 `300~500개`, 문제 번들 `900~1,500개` 기준 반영
2. `docs/develop/16_[co]frontend_mvp_development_plan.md` 수정
   - 도전모드 MVP를 검색 저항성이 있는 단서 묶음 중심으로 재정의
   - `랭크 문제은행 구축과 AI 제작 파이프라인` 작업 스트림 추가
   - 문제 데이터 요구사항에 `search_resistance_score`, `clue_bundle`, `visual_clue_assets`, `strong_hint` 등 필드 추가
   - `AI 제작비와 모델 전략` 기술 결정 섹션 추가
3. `docs/develop/17_[co]frontend_mvp_screen_flow.md` 수정
   - 챌린지 입장/브리핑/플레이 오버레이를 블라인드 드롭 기준으로 수정
   - `결정적 힌트 3회`와 단서 번들 구조를 화면 흐름에 반영
   - 텍스트 카드 중심 챌린지를 캐주얼 후보로 문서화
4. `docs/develop/19_[co]frontend_mvp_checklist.md` 수정
   - 메인 랭크 모드의 반검색 설계 기준 추가
   - 문제은행 규모, 난이도/검색저항성 분리, AI 제작 파이프라인 관련 체크 항목 추가
5. `docs/02_Project_Structure.md` 업데이트
   - 변경된 문서 성격에 맞춰 각 개발 문서 설명을 보정

### 판단 근거
- 메인 랭크 모드는 `설명 읽기`보다 `단서 조합 후 지구본에 행동하기`가 먼저여야 한다.
- 난이도와 검색 저항성은 별도로 관리해야 하며, AI 난이도는 초기 추정치일 뿐 실제 티어는 운영 데이터로 교정해야 한다.
- 1인 개발 체제에서는 원천 엔터티 전체를 문제화하는 전략보다, 강한 엔터티 `300~500개`를 먼저 선별해 복수 난이도 번들로 운영하는 편이 현실적이다.
- 제작비 측면에서도 고비용 추론 모델을 전량 생성에 쓰기보다, 저비용 고속 모델 생성 + 중간급 검수 + AI 이미지 스타일화 구조가 훨씬 합리적이다.

### 다음 작업 제안
1. 랭크용 단서 번들 스키마와 금지어 규칙을 먼저 잠그기
2. `300~500개` 랭크용 문제은행 후보 선별 기준을 문서화하기
3. 이후 점수 규칙과 리더보드 저장 구조를 새 메인 경쟁 모드 기준으로 재설계하기

## [2026-03-07] [co] 랭크용 단서 번들 스키마 문서화

### 작업 배경
메인 경쟁 모드를 `블라인드 드롭` 계열로 재설계한 뒤에는, 실제 문제 1건이 어떤 데이터를 가져야 하는지 먼저 고정할 필요가 생겼다. 이 단계를 건너뛰면 이후 AI 생성 프롬프트, 검수 규칙, 리더보드 저장 구조가 모두 흔들리게 된다. 특히 1인 개발 체제에서는 `정답 엔터티`, `단서 번들`, `결정적 힌트`, `난이도`, `검색 저항성`을 명시적으로 분리한 공통 스키마가 있어야 생산 파이프라인을 자동화하기 쉽다.

### 변경 내용
1. `docs/develop/20_[co]ranked_clue_bundle_schema.md` 작성
   - 랭크용 문제의 공통 구조를 `answer_entity + ranked_clue_bundle` 관계로 정의
   - 정답 엔터티 최소 필드와 랭크용 단서 번들 JSON 예시 추가
   - `difficulty`, `search_resistance`, `visual_clues`, `text_clue`, `strong_hint`, `quality`, `generation` 필드 구조 확정
   - 금지 규칙, 품질 기준, 난이도 보정 원칙, AI 생성/검수 파이프라인 연결 방식 정리
   - `소크라테스` 예시로 실제 번들 형태를 설명
2. `docs/develop/19_[co]frontend_mvp_checklist.md` 업데이트
   - `랭크용 단서 번들의 공통 스키마 확정` 항목 완료 처리
   - 다음 우선순위를 `금지어 규칙` 확정으로 한 단계 구체화
3. `docs/02_Project_Structure.md` 업데이트
   - 신규 스키마 문서를 구조도에 반영

### 판단 근거
- 정답 엔터티와 문제 번들을 분리해야 같은 정답에 여러 난이도 번들을 붙일 수 있다.
- 문제 스키마가 먼저 고정돼야 AI 생성 프롬프트와 검수 규칙을 일관되게 설계할 수 있다.
- 초기 단계에서는 정규화 + JSONB 혼합 구조가 가장 현실적이며, 스키마 변동 가능성이 높은 `clue_payload`는 JSONB로 두는 편이 안전하다.

### 다음 작업 제안
1. 랭크용 단서 번들 `금지어/금지패턴 규칙표`를 별도 문서로 잠그기
2. `300~500개` 랭크용 문제은행 후보 선별 기준 문서를 작성하기
3. 이후 점수 규칙과 리더보드 저장 구조를 새 스키마에 맞춰 정리하기

## [2026-03-07] [co] 랭크용 단서 생성 규칙과 문제은행 후보 선별 기준 문서화

### 작업 배경
랭크용 단서 번들 스키마를 고정한 뒤에는, 실제 생성 단계에서 무엇을 허용하고 무엇을 금지할지, 또 어떤 엔터티를 애초에 랭크 문제 후보로 올릴지 문서로 분리할 필요가 생겼다. 이 두 단계가 없으면 스키마는 있어도 품질이 흔들린다. 특히 1인 개발 체제에서는 생성 규칙, 금지어, 카테고리별 템플릿, 후보 선별 퍼널을 문서화해두지 않으면 이후 AI 출력 품질이 일관되지 않게 된다.

### 변경 내용
1. `docs/develop/21_[co]ranked_clue_generation_rules.md` 작성
   - 공개 단서, 결정적 힌트, 금지어, 금지 패턴 규칙 정리
   - 카테고리별 단서 템플릿 작성
   - 난이도별 단서 조합 원칙 정리
   - 생성 프롬프트와 검수 프롬프트 골격 추가
2. `docs/develop/22_[co]ranked_candidate_selection_criteria.md` 작성
   - 20,000개 원천 엔터티에서 `300~500개` 승인 엔터티를 뽑기 위한 퍼널 정의
   - 하드 필터, 스코어링 기준, 승인/보류/제외 기준 정리
   - 카테고리/시대/지역 분포 권장안 추가
   - 승인 예시, 보류 예시, 제외 예시 정리
3. `docs/develop/19_[co]frontend_mvp_checklist.md` 업데이트
   - 금지어 규칙, 후보 선별 기준 관련 항목 완료 처리
   - 폐기 기준과 생성 프롬프트 규칙도 반영
4. `docs/02_Project_Structure.md` 업데이트
   - 신규 문서 2개를 구조도에 반영

### 판단 근거
- 스키마만으로는 문제 품질을 통제할 수 없고, 생성 규칙과 선별 기준이 같이 있어야 한다.
- 랭크 모드는 특히 `검색 저항성`과 `추론 가능성`을 동시에 만족해야 하므로 금지 패턴 규칙이 필수다.
- 후보 선별을 먼저 거칠게 잘라야 이후 AI 생성 비용과 사람 검수 비용을 모두 줄일 수 있다.

### 다음 작업 제안
1. 결과 점수 규칙과 힌트 패널티 규칙을 정리하기
2. 일일 챌린지 저장/리더보드 최소 스키마를 정의하기
3. 이후 실제 문제 생성용 프롬프트 파일과 검수 스크립트 초안을 만들기

## [2026-03-07] [co] person 데이터 기준 랭크 파일럿 후보군 1차 선별

### 작업 배경
전체 데이터셋이 아직 수집 중이라 `place / event / building / disaster`까지 포함한 최종 랭크 문제은행을 확정하기엔 이르다. 반면 `person`은 상대적으로 확보가 앞서 있기 때문에, 지금 단계에서는 최종 300~500개 승인 풀을 만들기보다 `person`만으로도 문제 생성-검수-폐기 루프가 성립하는지 먼저 검증할 필요가 있었다.

### 변경 내용
1. `docs/develop/23_[co]person_only_ranked_pilot_candidates.md` 작성
   - 현재 `person` 데이터만으로 파일럿 문제군을 만들기 위한 1차 승인/보류 후보를 정리
   - 데이터 상태(`3,580` person 카드, 좌표/요약/이미지 보유율)와 편중(유럽/현대/인물문화 위주)을 기록
   - `approved_now 24명`, `hold 8명`, `2차 검토 우선순위` 목록을 확정
   - 실제 문제 생성용 첫 배치를 `3개 묶음 x 8명`으로 나눠 제안
2. `docs/develop/19_[co]frontend_mvp_checklist.md` 업데이트
   - `person` 데이터 기준 파일럿 랭크 후보군 1차 선별 항목을 완료 처리
   - 다음 즉시 작업을 `인물 1차 승인 후보 24명의 첫 단서 번들 배치 생성`으로 명시
3. `docs/02_Project_Structure.md` 업데이트
   - 신규 문서를 구조도에 반영하고 마지막 업데이트 일자를 갱신

### 판단 근거
- `person`만으로도 파일럿 문제군은 만들 수 있지만, 최종 랭크 문제은행을 person만으로 채우는 것은 맞지 않다.
- 강한 person 문제는 주로 `정복`, `항해`, `발명`, `군사`, `제도 구축` 축에서 나오고, 철학자/종교 창시자/개념형 인물은 보류 비율이 높아질 수밖에 없다.
- 따라서 지금은 `최종 풀 확정`보다 `approved_now 후보 24명으로 실제 단서 번들을 뽑아 usable/revise/reject를 재판정`하는 편이 더 현실적이다.

### 다음 작업 제안
1. `approved_now 24명` 기준으로 첫 단서 번들 배치를 실제 생성하기
2. 생성 결과를 `usable / revise / reject`로 재분류하기
3. 이후 `place / event` 데이터가 안정화되면 사람 중심 파일럿 풀을 혼합 문제은행으로 확장하기

## [2026-03-07] [co] 랭크 문제은행 지역 가중치에 한국 우선도 반영

### 작업 배경
랭크 모드의 정체성은 글로벌 역사 탐험이지만, 초기 타깃 사용자는 한국 사용자다. 따라서 완전 중립적인 지역 분포만 고집하면 국내 초기 사용자가 서비스에 진입할 때 익숙한 접점이 부족해질 수 있다. 반대로 한국 문제 비중을 과하게 높이면 글로벌 정체성이 흔들린다. 이 균형점을 문서에 명시할 필요가 생겼다.

### 변경 내용
1. `docs/develop/15_[co]frontend_mvp_product_plan.md` 수정
   - 글로벌 문제은행을 유지하되, 초기 운영에서는 한국 관련 엔터티와 동아시아 문제에 약한 가중치를 준다는 원칙 추가
2. `docs/develop/22_[co]ranked_candidate_selection_criteria.md` 수정
   - 품질 점수와 한국 가중치를 분리
   - 최종 승인 풀 기준 `한국 관련 엔터티 8~12%`, `동아시아 전체 18~25%` 권장안 추가
   - 동점권 승인 시 한국/동아시아를 약하게 우선할 수 있다는 운영 규칙 추가
3. `docs/develop/23_[co]person_only_ranked_pilot_candidates.md` 수정
   - 인물 파일럿 선별에서도 한국 관련 인물을 약간 우선하는 원칙을 명시
   - 현재 파일럿 24명 중 한국 관련 인물 3명을 포함한 이유를 기록
4. `docs/develop/19_[co]frontend_mvp_checklist.md` 수정
   - 한국 관련 엔터티 비중과 초기 운영 가중치 원칙 반영 항목 완료 처리
5. `docs/02_Project_Structure.md` 업데이트
   - 22번 문서 설명을 실제 역할에 맞게 보정

### 판단 근거
- 초기 국내 사용자는 완전히 낯선 지역 문제만 연속으로 만나면 서비스 진입 장벽이 높아질 수 있다.
- 그렇다고 한국 편향형 문제은행으로 가면 TimeGlobe의 글로벌 역사 탐험 정체성이 약해진다.
- 따라서 `품질 점수는 글로벌 기준`, `최종 선발과 시퀀싱은 한국 가중치 반영` 구조가 가장 균형이 좋다.

### 다음 작업 제안
1. `approved_now 24명` 기준으로 첫 단서 번들 배치를 실제 생성하기
2. 생성된 번들에도 한국 가중치가 과하지 않은지 함께 점검하기
3. 이후 `daily / weekly` 시드 구성 규칙에 한국/동아시아 시퀀싱 기준을 반영하기

## [2026-03-07] [co] 문제은행 규모 목표를 단계별 unique entity 기준으로 수정

### 작업 배경
기존 문서에는 초기 랭크 문제은행을 `300~500개` 수준으로 보는 표현이 남아 있었지만, 실제 반복 플레이를 상정하면 이 숫자는 내부 테스트 또는 클로즈드 베타 정도에만 맞는다. 같은 정답 엔터티의 여러 번들은 사용자 입장에선 사실상 같은 문제로 느껴질 수 있기 때문에, 콘텐츠 풀 규모는 `문제 번들 수`가 아니라 `unique answer entity 수`를 기준으로 다시 잡아야 했다.

### 변경 내용
1. `docs/develop/15_[co]frontend_mvp_product_plan.md` 수정
   - 문제은행 전략을 `번들 수`보다 `unique answer entity 수` 중심으로 재정의
   - 단계별 목표를 `내부 테스트 500 -> 공개 베타 3,000~5,000 -> 유료화 20,000 -> 장기 100,000`으로 정리
2. `docs/develop/16_[co]frontend_mvp_development_plan.md` 수정
   - AI 제작 파이프라인과 콘텐츠 구축 스트림의 목표 수치를 단계별로 조정
   - 랭크 중복 방지 기준이 `bundle_id`가 아니라 `answer_entity_id`임을 명시
3. `docs/develop/19_[co]frontend_mvp_checklist.md` 수정
   - 콘텐츠 규모 체크 항목을 단계별 unique entity 기준으로 수정
   - 내부 테스트 `500 entity` 확보 계획과 공개 베타 `3,000~5,000 entity` 확장 로드맵을 후속 작업으로 추가
4. `docs/develop/22_[co]ranked_candidate_selection_criteria.md` 수정
   - 문서 목적과 퍼널을 단계별 규모 목표에 맞게 조정
   - 내부 테스트 `500개` 승인 풀 기준으로 표현 정리
5. `docs/develop/23_[co]person_only_ranked_pilot_candidates.md` 수정
   - person-only 파일럿의 목적이 최종 출시용 풀 확정이 아니라 생산 루프 검증임을 더 명확히 정리
6. `docs/02_Project_Structure.md` 업데이트
   - 22번 문서 설명을 실제 역할에 맞게 보정

### 판단 근거
- `500개`는 내부 테스트나 클로즈드 베타 기준으로는 유효하지만, 공개 베타 이상의 반복 플레이를 버티기엔 부족하다.
- 같은 정답 엔터티의 다른 번들은 학습/복습엔 유용하지만, 랭크용 반복 콘텐츠 풀을 크게 늘려주지 못한다.
- 따라서 콘텐츠 전략은 `정답 엔터티 다양성` 중심으로 바뀌어야 하며, 공개 베타 이후에는 최소 `3,000개` 이상을 목표로 해야 한다.

### 다음 작업 제안
1. `approved_now 24명` 기준으로 첫 단서 번들 배치를 실제 생성하기
2. 내부 테스트용 `500 entity` 확보 계획을 카테고리별로 분해하기
3. 공개 베타용 `3,000~5,000 entity` 확장 로드맵을 분야별로 문서화하기

## [2026-03-07] [co] person 파일럿 배치 A 8명 랭크 번들 초안 작성

### 작업 배경
`person` 데이터만으로도 랭크용 문제 생성 루프가 실제로 성립하는지 확인해야 했다. 이 단계의 목적은 최종 문제은행 확정이 아니라, `approved_now` 후보군에서 실제 단서 번들을 뽑았을 때 `usable / revise / reject`로 다시 판정할 수 있는지 보는 것이다. 사용자와의 수동 플레이 테스트에서 `장영실 ↔ 세종`, `구텐베르크 ↔ 활자 인쇄술`, `갈릴레오 ↔ 지동설`처럼 인물보다 개념이나 후원자가 먼저 떠오르는 문제가 확인됐기 때문에, 초안 단계부터 `인물 고유성`과 `역할 충돌`을 명시적으로 검토할 필요가 있었다.

### 변경 내용
1. `docs/develop/24_[co]ranked_pilot_batch_a_bundle_drafts.md` 추가
   - 배치 A 8명(`진시황`, `클레오파트라`, `아소카`, `징기스칸`, `티무르`, `만사 무사`, `세종`, `이순신`)의 랭크용 단서 번들 초안을 작성
   - 각 초안에 `difficulty_tier`, `search_resistance_score`, `visual_clues`, `text_clue`, `strong_hint`, `risk_notes`, `revision_notes`를 포함
   - 초안 수준에서 `draft_usable`와 `draft_revise`를 나눠 배치 A 중간 판정을 기록
2. `docs/develop/19_[co]frontend_mvp_checklist.md` 업데이트
   - `인물 파일럿 배치 A 8명`의 첫 단서 번들 초안 생성을 완료 처리
   - 다음 우선순위를 `24명 전체 첫 배치 생성`으로 유지
3. `docs/02_Project_Structure.md` 업데이트
   - 신규 24번 문서를 구조도에 반영

### 판단 근거
- 인물 문제는 구조상 가능하지만, 좋은 문제는 `인물 정답`으로 수렴해야 하고 나쁜 문제는 `업적/개념/후원자`로 수렴한다.
- 배치 A 초안에서도 `클레오파트라`, `아소카`, `징기스칸`, `만사 무사`, `이순신`은 비교적 안정적이었고, `진시황`, `티무르`, `세종`은 역할 충돌과 역사 일반론으로 흐를 가능성이 높아 `revise-first`로 보는 것이 맞다.
- 따라서 지금 단계에선 많은 수를 빠르게 늘리기보다, 초안이라도 `usable / revise` 판정을 붙여 다음 생성 규칙을 다듬는 편이 생산성이 높다.

### 다음 작업 제안
1. 배치 A 8명 초안을 다시 검토해 `text_clue`와 `strong_hint`의 직설성/개념쏠림을 조정하기
2. 같은 기준으로 `approved_now 24명` 전체에 대해 첫 단서 번들을 이어서 생성하기
3. `500 entity` 카테고리 분해 계획은 사용자와 상의한 뒤 별도 문서로 진행하기

## [2026-03-07] [co] person 파일럿 24명 첫 단서 번들 배치 완성

### 작업 배경
배치 A 초안을 만든 뒤 수동 플레이 테스트에서 `장영실 ↔ 세종`, `구텐베르크 ↔ 활자 인쇄술`, `갈릴레오 ↔ 지동설`처럼 인물보다 업적·개념이 먼저 떠오르는 패턴이 확인됐다. 따라서 `approved_now 24명` 전체를 실제 번들로 확장하기 전에, 배치 A의 `revise` 항목을 먼저 고치고 같은 기준으로 배치 B/C를 작성해 `인물 문제의 약점`이 어디서 반복되는지 확인할 필요가 있었다.

### 변경 내용
1. `docs/develop/24_[co]ranked_pilot_batch_a_bundle_drafts.md` 수정
   - `진시황`, `티무르`, `세종대왕` 초안을 재작성
   - 세 항목을 `draft_revise`에서 `draft_usable`로 상향
   - 배치 A 중간 판정을 `8명 전원 usable 초안` 상태로 갱신
2. `docs/develop/25_[co]ranked_pilot_batch_b_bundle_drafts.md` 추가
   - 배치 B 8명(`헤론`, `이븐 알하이삼`, `알-자자리`, `구텐베르크`, `레오나르도`, `갈릴레오`, `장영실`, `마리 퀴리`)의 첫 단서 번들 초안 작성
   - 과학/발명 문제에서 `업적 일반론`으로 흐르는 위험을 문서화
   - `레오나르도 다 빈치`만 `draft_revise`로 보류
3. `docs/develop/26_[co]ranked_pilot_batch_c_bundle_drafts.md` 추가
   - 배치 C 8명(`마르코 폴로`, `이븐 바투타`, `콜럼버스`, `바스쿠 다 가마`, `마젤란`, `샤카 줄루`, `시몬 볼리바르`, `넬슨 만델라`)의 첫 단서 번들 초안 작성
   - 탐험가 축에서 `인물보다 항로/해협/개념이 먼저 떠오르는 위험`을 명시
   - `마르코 폴로`, `페르디난드 마젤란`을 `draft_revise`로 분류
4. `docs/develop/19_[co]frontend_mvp_checklist.md` 업데이트
   - `인물 1차 승인 후보 24명`의 첫 단서 번들 배치 생성 항목을 완료 처리
5. `docs/02_Project_Structure.md` 업데이트
   - 신규 25, 26번 문서를 구조도에 반영

### 판단 근거
- 인물 문제는 충분히 성립하지만, 과학자와 탐험가는 다른 종류의 리스크를 가진다.
- 과학자 문제는 `이론/발명품`이 먼저 떠오르고, 탐험가 문제는 `항로/도착지`가 먼저 떠오른다.
- 따라서 문제 품질을 올리려면 단순히 번들을 많이 찍는 것보다 `인물로 수렴하는 단서`와 `업적/개념으로 새는 단서`를 분리해 기록하는 편이 더 중요하다.
- 이번 24명 세트는 `배치 A usable 8`, `배치 B usable 7 revise 1`, `배치 C usable 6 revise 2` 수준으로 보는 것이 가장 솔직하다.

### 다음 작업 제안
1. `레오나르도`, `마르코 폴로`, `마젤란`의 `revise` 초안을 다시 써서 person 24명 세트를 1차 마감하기
2. person 24명 세트에서 실제 플레이 테스트를 더 돌려 `업적/개념 답안 유도율`을 확인하기
3. `500 entity` 확보 계획은 사용자와 상의한 뒤 별도 카테고리 문서로 진행하기

## [2026-03-07] [co] person 24명 랭크 번들의 이미지 생산성 리뷰 작성

### 작업 배경
랭크 문제의 텍스트 단서와 문제 구조는 좋아졌지만, 실제 운영에선 `이 이미지를 AI가 안정적으로 만들어낼 수 있는가`가 별도의 병목이다. 특히 `고대 기계`, `조선 계측기`, `특정 건축/복식 시대감`처럼 세부 고증이 필요한 단서는 순수 직생성 한 번으로 끝나지 않을 가능성이 높다. 따라서 현재 작성된 `person 24명` 번들을 `문제 품질`과는 별개로 `시각 자산 생산성` 관점에서 다시 판정할 필요가 생겼다.

### 변경 내용
1. `docs/develop/27_[co]ranked_visual_clue_feasibility_review.md` 추가
   - `image_feasibility(high/medium/low)` 기준 정의
   - `image_strategy(generate / style_from_reference / composite / rewrite_before_production)` 정의
   - `symbolic_illustration`, `stylized_scene`, `artifact_closeup`별 기본 생산 전략 정리
   - 현재 24명 번들 전체에 대한 이미지 생산성 판정표 작성
   - 이후 문제 작성에 바로 쓸 수 있는 시각 단서 작성 규칙을 명시
2. `docs/develop/19_[co]frontend_mvp_checklist.md` 업데이트
   - `대표 이미지와 상징 이미지 사용 규칙` 항목 완료 처리
   - `인물 24명 번들의 image_feasibility/image_strategy 1차 리뷰` 완료 처리
3. `docs/02_Project_Structure.md` 업데이트
   - 신규 27번 문서를 구조도에 반영

### 판단 근거
- 이번 24명 세트는 `high 12 / medium 12 / low 0`으로 보는 것이 맞다.
- 이 평가는 `전부 직생성 가능`을 뜻하지 않는다.
- 정확한 의미는 `절반 정도는 바로 생성 가능`, `절반 정도는 레퍼런스 기반 스타일화나 간단 합성이 필요`하다는 뜻이다.
- 가장 위험한 유형은 `artifact_closeup`가 핵심인 번들이다. 이런 문제는 이후에도 `고정밀 오브젝트는 최대 1개` 규칙을 유지하는 편이 좋다.

### 다음 작업 제안
1. `high` 판정 번들 6~10개를 실제 생성해 이미지 품질을 검증하기
2. `medium` 판정 번들에 필요한 공공도메인/CC0 레퍼런스 목록을 만들기
3. 이후 새 문제 번들을 쓸 때는 `3개 시각 단서 중 2개는 high-feasibility` 원칙을 기본 규칙으로 적용하기

## [2026-03-07] [co] high-feasibility 이미지 파일럿 세트 선정 및 생성 프롬프트 정리

### 작업 배경
이미지 생산성 리뷰 문서만으로는 바로 생성에 들어가기 어렵다. 실제로는 `어떤 인물 6~10개를 먼저 뽑을지`, `공통 스타일을 어떻게 고정할지`, `개별 카드 프롬프트를 어떻게 쓸지`가 한 번 더 정리돼야 한다. 지금 단계에선 자동화보다 `바로 생성 가능한 파일럿 세트`가 더 중요하므로, `high` 판정 번들 중에서 첫 라운드 샘플을 따로 묶을 필요가 있었다.

### 변경 내용
1. `docs/develop/28_[co]high_feasibility_image_pilot_set.md` 추가
   - 이미지 파일럿용 `8명` 선정
   - 공통 스타일 프롬프트, 공통 네거티브 프롬프트, 출력 규칙 정리
   - 인물별 `3개 카드` 생성 프롬프트 작성
   - 실제 생성 순서를 `만사 무사 -> 이순신 -> 구텐베르크 -> 마리 퀴리 -> 갈릴레오 -> 징기스칸 -> 이븐 알하이삼 -> 넬슨 만델라`로 제안
2. `docs/develop/19_[co]frontend_mvp_checklist.md` 업데이트
   - `high-feasibility 번들 기반 이미지 파일럿 세트 선정` 항목 완료 처리
3. `docs/02_Project_Structure.md` 업데이트
   - 신규 28번 문서를 구조도에 반영

### 판단 근거
- 파일럿은 `생성 성공률`과 `문제 단서 전달력`을 먼저 봐야 하므로, `high` 판정 번들만 쓰는 편이 맞다.
- 역사적 정밀 유물보다 `장면`, `분위기`, `행동`이 중심인 인물들이 첫 라운드 샘플로 적합하다.
- 따라서 만사 무사, 이순신, 구텐베르크, 마리 퀴리, 갈릴레오처럼 장면성이 강하고 카드 축소에도 읽히는 문제를 우선하는 편이 가장 안전하다.

### 다음 작업 제안
1. `28번 문서`의 첫 3명 정도를 실제 이미지 모델에 넣어 카드 축소 품질을 확인하기
2. 실패한 프롬프트는 금지 패턴이나 문장 규칙으로 다시 환류하기
3. 그다음 `medium` 판정 번들용 레퍼런스 확보 목록을 만들기

## [2026-03-07] [co] 이미지 프롬프트 작성 로직 문서화 및 2차 파일럿 샘플 확장

### 작업 배경
이미지 파일럿을 실제로 돌려보니, 개별 프롬프트를 감각적으로 조금씩 만지는 방식만으로는 재현 가능한 생산 라인을 만들기 어렵다는 점이 분명해졌다. 특히 `구텐베르크`처럼 문자와 정밀 오브젝트가 섞이는 문제는 프롬프트 문장 하나보다 `무엇을 그리게 하고 무엇을 금지할지`를 슬롯 단위로 통제하는 편이 더 중요했다.

### 변경 내용
1. `.cache/image-pilot/`에 2차 샘플 추가
   - `marie_curie_card1.png`, `marie_curie_card1_512.png`
   - `galileo_card1.png`, `galileo_card1_512.png`
   - `genghis_khan_card1.png`, `genghis_khan_card1_512.png`
2. `docs/develop/29_[co]image_prompt_logic.md` 추가
   - `단서 -> 이미지 브리프 -> 생성 프롬프트` 작성 파이프라인 정의
   - `scene / focus / count / mood / anti-failure / style` 6슬롯 구조 정의
   - 반복 오브젝트, 가짜 문자, 포스터형 영웅 구도, 과도한 전경 장면 등 금지 패턴 정리
   - `symbolic scene`, `tool/workbench close-up`, `environment-as-strategy`, `abstract scientific metaphor` 유형별 작성 로직 정리
3. `docs/develop/19_[co]frontend_mvp_checklist.md` 업데이트
   - 이미지 프롬프트 작성 로직 정리 항목 완료 처리
4. `docs/02_Project_Structure.md` 업데이트
   - 신규 29번 문서를 구조도에 반영

### 판단 근거
- 지금 단계의 병목은 더 좋은 수식어를 찾는 게 아니라, 생성 모델이 잘 처리하는 프롬프트 슬롯 구조를 고정하는 것이다.
- `count constraint`와 `anti-failure constraint`를 분리해서 넣는 방식이 반복 인물, 반복 탈것, 언캐니한 대군 장면을 줄이는 데 가장 직접적이다.
- 과학자/발명가 문제는 초상보다 `실험`, `관측`, `작업대`, `상징 메타포` 중심으로 갈수록 안정적이다.

### 다음 작업 제안
1. 현재 `high` 파일럿 전체를 29번 문서의 6슬롯 구조로 통일하기
2. `medium` 번들에 대해서는 레퍼런스 필요 슬롯을 별도로 표시하기
3. 카드 축소 뷰 기준 `통과 / 보류 / 재작성` 판정표를 만들기

## [2026-03-07] [co] 마리 퀴리 시각 단서의 역사 개연성 보정

### 작업 배경
사용자 피드백에서 `마리 퀴리` 시각 단서가 남성 과학자처럼 읽힐 위험과 함께, `큰 솥 같은 실험 용기` 표현이 근대 과학자보다 연금술/중세 실험실 쪽으로 오독될 수 있다는 지적이 나왔다. 이 피드백은 개별 프롬프트 수정이 아니라, `정답 정체성과 충돌하는 인물 암시`와 `generic era slippage`를 함께 금지 규칙으로 끌어올려야 한다는 뜻이다.

### 변경 내용
1. `docs/develop/25_[co]ranked_pilot_batch_b_bundle_drafts.md` 수정
   - 마리 퀴리 시각 단서 2를 `큰 솥 같은 실험 용기`에서 `검은 광석 자루와 잔류물 샘플, 산 자국이 남은 대형 처리 용기` 중심으로 보정
2. `docs/develop/28_[co]high_feasibility_image_pilot_set.md` 수정
   - 마리 퀴리 카드 2 프롬프트를 `광석/잔류물/산업적 정제 노동` 중심으로 수정
   - `no medieval cauldron`, `no alchemy imagery`를 명시
3. `docs/develop/29_[co]image_prompt_logic.md` 수정
   - `generic era slippage` 금지 패턴 추가
   - 근대 과학자를 연금술풍으로 오독시키지 않는 재료/환경 앵커 규칙 추가

### 판단 근거
- 역사적으로 주철 가마류가 완전히 틀린 건 아니어도, 문제 카드에서 그 표현은 잘못된 시대 연상을 부르는 비용이 더 크다.
- 마리 퀴리의 경우 `광석`, `잔류물`, `거친 정제 노동`, `산업적 연구 환경`이 더 강한 정답 수렴 단서다.
- 게임 문제는 학술적 디테일 하나보다 `사용자가 어떤 시대/직업군으로 읽는지`가 더 중요하므로, 오독 비용이 큰 표현은 빨리 제거하는 편이 맞다.

## [2026-03-07] [co] 퀴즈 생성 규칙에 시대 반영 및 성별 오인 금지 추가

### 작업 배경
사용자 피드백에서 드러난 문제는 단순히 이미지 프롬프트의 표현 문제가 아니었다. 퀴즈 생성 단계에서부터 `정답 인물이 살았던 시대를 반영한다`, `성별이 중요한 힌트가 될 경우 오해를 줄 수 있는 프롬프트 생성을 금지한다`는 규칙이 명시되어야 한다는 요구가 생겼다.

### 변경 내용
1. `docs/develop/21_[co]ranked_clue_generation_rules.md` 수정
   - 핵심 원칙에 `시대 반영`과 `성별 오인 금지` 추가
   - 시각 단서 필수 조건에 `시대/물질문화 충돌 금지` 추가
   - 금지 패턴과 자동 검수 질문에 `정답 시대 충돌`, `반대 성별 오인` 항목 추가
   - 인물 단서 템플릿과 생성 프롬프트 골격에도 같은 제약 반영
2. `docs/develop/29_[co]image_prompt_logic.md` 수정
   - `시대와 정체성의 개연성 바닥선` 원칙 추가
   - `identity contradiction` 금지 패턴 추가
   - `no visible person`, `hands only`, `indistinct silhouette only` 같은 제약을 정체성 충돌 회피 수단으로 명시

### 판단 근거
- 정답을 직접 노출하지 않는 것과, 정답과 모순되는 잘못된 인상을 주지 않는 것은 별개의 문제다.
- 특히 인물 문제는 사용자가 단서를 조합하며 직업, 시대, 성별까지 함께 추론하므로, 잘못된 정체성 암시는 곧 문제 불공정으로 이어진다.
- 따라서 이 규칙은 이미지 생성 후처리가 아니라 퀴즈 생성 상위 규칙으로 다뤄야 한다.

## [2026-03-07] [co] 마리 퀴리 번들 및 이미지 프롬프트 재생성

### 작업 배경
사용자 피드백에서 기존 마리 퀴리 카드가 남성 연구자를 암시할 수 있다는 점, 그리고 `솥 같은 실험 용기` 표현이 연구 대상인 광석 정제보다 연금술풍 오독을 더 강하게 만든다는 점이 드러났다. 따라서 마리 퀴리는 `여성 과학자 문제의 기준 케이스`로 다시 손봐야 했다.

### 변경 내용
1. `docs/develop/25_[co]ranked_pilot_batch_b_bundle_drafts.md` 수정
   - 시각 단서 1을 `광석 조각 + 침전물 + 시료병 + 연구 노트` 작업대 클로즈업으로 보강
   - 텍스트 단서를 `광석 더미에서 꺼낸 희미한 빛` 중심으로 보정
   - 위험 메모에 `남성 연구자 암시 금지`, `generic lab 금지` 추가
2. `docs/develop/28_[co]high_feasibility_image_pilot_set.md` 수정
   - 마리 퀴리 카드 1~3 프롬프트를 모두 재생성
   - 카드 1, 카드 2, 카드 3에 `no visible person`을 직접 명시
   - 광석, 잔류물, 산 자국, 산업적 처리 환경을 더 명확히 고정
3. `docs/develop/29_[co]image_prompt_logic.md` 수정
   - 성별 오인이 큰 리스크인 인물 문제에서는 공개 카드에 `no visible person`을 기본값으로 둘 수 있다는 규칙 추가

### 판단 근거
- 마리 퀴리 문제의 핵심은 `빛나는 물질`만이 아니라 `광석 정제`, `거친 처리 노동`, `몸의 대가`를 함께 떠올리게 하는 데 있다.
- 이 케이스는 성별 오인 금지 규칙이 실제 프롬프트 레벨에서 어떻게 적용돼야 하는지를 보여주는 좋은 기준 사례다.

## [2026-03-07] [co] Gemini 모델별 역사 퀴즈 번들 드라이런 스크립트 추가

### 작업 배경
사용자 요구가 `문제 자체를 AI에게 생성시키고, 모델별로 품질을 비교해 가성비 좋은 모델을 고르는 것`으로 명확해졌다. 따라서 먼저 필요한 것은 모델별 결과를 같은 입력과 같은 출력 스키마로 비교할 수 있는 드라이런 스크립트다.

### 변경 내용
1. `scripts/curation/quizBundleModelDryRun.mjs` 추가
   - TimeGlobe 전용 `문제 작가` 시스템 프롬프트 정의
   - 샘플 타깃 5개(`마리 퀴리`, `구텐베르크`, `장영실`, `양자역학`, `안사의 난`) 기본 탑재
   - 기본 동작은 `dry run`으로 시스템 프롬프트와 사용자 프롬프트를 `.cache/quiz-bundle-dryrun/<timestamp>/`에 저장
   - `--run --models ...`로 실제 Gemini 모델들을 순차 호출하고 raw/parsed JSON 및 usage metadata를 저장
2. `package.json` 업데이트
   - `npm run quiz:bundle-dryrun` 스크립트 추가
3. `docs/02_Project_Structure.md` 업데이트
   - 신규 드라이런 스크립트를 구조도에 반영

### 판단 근거
- 모델별 품질 비교를 하려면 입력 프롬프트가 완전히 같아야 한다.
- 지금 단계에서는 비용 계산보다 먼저 `같은 프롬프트에서 어떤 모델이 원하는 게임 작가 톤을 더 잘 내는지`를 보는 게 맞다.
- 그래서 디폴트는 네트워크 없는 dry run, 이후 필요할 때만 실제 호출하는 구조가 가장 안전하다.

## [2026-03-07] [co] 퀴즈 생성 기본 모델을 Gemini 3 Flash Preview로 확정하고 파이프라인/큐레이션 정책 문서화

### 작업 배경
문제 생성 실험 결과, `gemini-2.5-pro`는 문장 자체는 더 화려했지만 후속 이미지 브리프와 이미지 생성으로 번역할 때 해석 여지가 더 컸다. 반면 `gemini-3-flash-preview`는 더 정적이고 이미지화하기 쉬운 장면 단서를 내줬고, 실제 토큰 비용도 낮았다. 따라서 현재 목표인 `문제 -> 이미지 브리프 -> 이미지 생성` 파이프라인에는 `gemini-3-flash-preview`가 더 맞다는 결론을 내렸다.

### 변경 내용
1. `scripts/curation/quizBundleModelDryRun.mjs` 수정
   - 기본 비교 모델을 `gemini-3-flash-preview` 중심으로 정리
2. `docs/develop/30_[co]quiz_generation_pipeline.md` 추가
   - 문제 생성 기본 모델 확정
   - `5개 엔터티 = 1배치` 기준
   - 생성 -> 구조 검수 -> 게임성 검수 -> 이미지화 적합성 검수 -> 이미지 브리프 변환 -> 대표 카드 파일럿 -> 승인 단계 고정
3. `docs/develop/31_[co]quiz_curation_policy.md` 추가
   - `approved_for_ranked / practice_only / hold_for_rewrite / rejected` 상태 정의
   - 랭크 중복 기준을 `answer_entity`로 고정
   - 좋은 문제, 보류 문제, 폐기 문제 기준 정리
4. `docs/develop/16_[co]frontend_mvp_development_plan.md`, `docs/develop/19_[co]frontend_mvp_checklist.md`, `docs/02_Project_Structure.md` 업데이트
   - 새 운영 기준과 문서 참조 반영

### 판단 근거
- 현재 병목은 `좋은 문장`이 아니라 `좋은 문제를 안정적으로 후속 이미지화하는 것`이다.
- 이 관점에서 `gemini-3-flash-preview`가 가장 현실적이다.
- 초기 운영에서는 전용 학습 모델보다도, 생성/검수/보류/폐기 기준을 명확히 하는 것이 더 중요하다.

## [2026-03-07] [co] 내부 테스트용 500 entity 확보 계획 문서화

### 작업 배경
문제 생성 모델과 큐레이션 정책을 정한 뒤에는, 실제로 내부 테스트용 `500 unique answer entities`를 어떤 구성과 순서로 채울지 별도 계획이 필요했다. 단순 비율표만 있으면 현재 데이터 현실과 확보 순서가 빠져 실행력이 떨어진다.

### 변경 내용
1. `docs/develop/32_[co]internal_test_500_entity_plan.md` 추가
   - 최종 목표 분포를 `장소 110 / 사건 110 / 재난 80 / 인물 90 / 발견·발명 60 / 기타 50`으로 고정
   - 지역/시대 분포 목표 정리
   - `120 -> 250 -> 500` 확보 웨이브 계획 추가
   - 한국 관련 엔터티 목표치를 `50개 전후`로 정리
   - 현재 데이터 현실상 `person`에서 시작하되 최종 풀은 혼합 구성으로 가야 한다는 점 명시
2. `docs/develop/19_[co]frontend_mvp_checklist.md` 업데이트
   - `내부 테스트용 500 entity 확보 계획` 항목 완료 처리
3. `docs/develop/16_[co]frontend_mvp_development_plan.md`, `docs/02_Project_Structure.md` 업데이트
   - 새 계획 문서 참조 반영

### 판단 근거
- 내부 테스트 500은 단순한 숫자 목표가 아니라, 구조 검증이 가능한 최소 혼합 풀이어야 한다.
- 지금처럼 `person` 데이터만 앞서 있는 상황에선 최종 분포와 실제 확보 순서를 함께 설계해야 한다.
- 공개 베타용 `3,000~5,000` 계획은 이 500 구조가 먼저 검증된 뒤에 가는 게 맞다.

## [2026-03-07] [co] 내부 테스트 선행 must-have 50 후보군 확정

### 작업 배경
`500 entity` 계획은 수립됐지만, 실제 제작 순서를 앞당기려면 그 전에 `지금 당장 문제 생성과 이미지 파일럿에 넣을 수 있는 50개`를 먼저 고정할 필요가 있었다. 이 세트는 최종 500의 축소판이라기보다, 현재 데이터 가용성을 반영한 `pre-wave 0` 실행 세트다.

### 변경 내용
1. `docs/develop/33_[co]internal_test_must_have_50_candidates.md` 추가
   - `person 18 / event-polity 15 / place 12 / battle-disaster 5` 구성의 실제 후보군 50개를 확정
   - 인물은 기존 승인군 문서를 앵커로 사용
   - 발명/작품 축은 현재 데이터 품질상 보류한다는 점을 명시
2. `docs/develop/32_[co]internal_test_500_entity_plan.md` 수정
   - `must-have 50`를 `pre-wave 0`로 보고, 실제 실행 세트는 33번 문서를 기준으로 본다는 점을 추가
3. `docs/develop/19_[co]frontend_mvp_checklist.md`, `docs/02_Project_Structure.md` 업데이트
   - 선행 후보군 선정 완료 처리 및 문서 구조 반영

### 판단 근거
- 첫 50은 이론상 균형보다 `지금 바로 생성 가능한가`가 더 중요하다.
- 현재는 `person`, `event/polity`, `place`가 가장 준비돼 있고, `invention`, `artwork`는 아직 거칠다.
- 따라서 선행 50은 availability bias를 허용하고, 120 단계에서 다시 균형을 맞추는 편이 실무적으로 맞다.

## [2026-03-07] [co] 내부 테스트용 500 후보 큐와 문제 배치 러너 추가

### 작업 배경
수작업 `must-have 50`만으로는 이제 충분하지 않았다. 사용자가 `완벽한 선별보다 빠른 반복 테스트`를 원했기 때문에, 로컬 데이터에서 거칠게라도 `500 candidate queue`를 만들고 이를 5개 단위로 Gemini 문제 번들 생성에 태워볼 수 있는 실행 파이프라인이 필요했다.

### 변경 내용
1. `scripts/curation/buildInternalTest500Candidates.mjs` 추가
   - `persons_cards`, `events_raw`, `places_sample`를 읽어 내부 테스트용 `500 후보 큐`를 자동 생성
   - 현재 결과: `person 90 / place 120 / polity 233 / battle_disaster 53 / seed_event 4`
   - 출력 파일: `data/curation/quiz_candidates/internal_test_500_candidates.json`
2. `scripts/curation/runQuizBundleBatches.mjs` 추가
   - `5개 엔터티` 단위로 문제 번들을 배치 생성
   - batch 디렉터리, prompt, parsed JSON, usage, state 파일을 함께 저장
   - `--resume` 기반 재개 지원
3. `scripts/curation/quizBundleModelDryRun.mjs` 수정
   - 공유 함수 export 가능하게 정리해 배치 러너가 재사용할 수 있게 함
4. `package.json` 업데이트
   - `quiz:candidates:500`
   - `quiz:bundle:batch`
5. `docs/develop/34_[co]quiz_batch_execution.md` 추가
   - 후보 큐 생성과 배치 실행 기준 정리

### 검증 결과
- `10배치 / 50엔터티` 실배치 실행 완료
- 모든 배치 `parsed_ok=true`
- state 파일 정상 기록
- 검증 세션: `.cache/quiz-bundle-batches/internal-test-500/20260307-195704`

### 판단 근거
- 지금 단계에선 `500`의 내용이 완벽하게 아름다워야 하는 게 아니라, `후보 큐 -> Gemini 문제 생성 -> parsed json 저장` 경로가 안정적으로 작동하는지가 중요하다.
- 배치 실행이 실제로 문제없이 돌아가는 것이 확인됐으므로, 다음 반복은 `문제 품질 라벨링`과 `실패 패턴 수집`에 더 집중하면 된다.

## [2026-03-07] [co] 내부 테스트용 500 문제 배치 전체 실행 완료

### 작업 배경
초기 `10배치 / 50엔터티` 검증만으로는 실제 내부 테스트 풀을 확보했다고 보기 어려웠다. 사용자가 바로 `500개 확보 후 파이프라인 검증`을 원했기 때문에, 같은 큐를 `resume`으로 끝까지 밀어 실제 완주 가능성과 누적 토큰 규모를 확인했다.

### 변경 내용
1. `docs/02_Project_Structure.md` 트리 표기 오타 수정
   - `33_[co]internal_test_must_have_50_candidates.md`
   - `34_[co]quiz_batch_execution.md`
2. `scripts/curation/runQuizBundleBatches.mjs`를 `--resume`으로 재개
   - 기존 state `nextIndex=50`에서 시작
   - 추가 `90배치 / 450엔터티` 실행
3. `docs/develop/34_[co]quiz_batch_execution.md` 업데이트
   - 최종 `100배치 / 500엔터티` 완료 상태 반영
   - 토큰 총량과 평균 사용량 기록
4. `docs/develop/19_[co]frontend_mvp_checklist.md` 업데이트
   - `500 후보 큐 전체 실행 검증 완료` 체크 처리

### 검증 결과
- 최종 state:
  - `nextIndex = 500`
  - `completedBatches = 100`
- 세션:
  - 초기 10배치: `.cache/quiz-bundle-batches/internal-test-500/20260307-195704`
  - 재개 90배치: `.cache/quiz-bundle-batches/internal-test-500/20260307-200858`
- 전체 집계:
  - `100배치 / 500엔터티`
  - `parsed_ok = true` 실패 `0`
  - prompt tokens `187,639`
  - output tokens `246,063`
  - thoughts tokens `119,828`
  - total tokens `553,530`
  - 평균 total tokens `5,535 / batch`

### 판단 근거
- 이제 `500 후보 전체`를 한 번 돌려도 파이프라인이 흔들리지 않는다는 것은 확인됐다.
- 다음 병목은 생성 안정성이 아니라 `문제 품질 큐레이션`, `직접적인 히든 힌트`, `이미지화 실패 패턴`이다.
- 따라서 다음 반복은 시스템 설계보다 산출물 라벨링과 프롬프트 미세 조정에 집중하는 편이 맞다.

## [2026-03-07] [co] 퀴즈 배치 결과 Markdown export 추가

### 작업 배경
`parsed.json`만으로는 사람이 결과를 빠르게 훑기 불편했다. 사용자가 결과를 `md` 파일 형태로도 따로 저장해 달라고 요청했기 때문에, 이후 배치 실행 시 Markdown을 자동 생성하고 기존 500개 결과도 역변환할 필요가 있었다.

### 변경 내용
1. `scripts/curation/quizBundleMarkdown.mjs` 추가
   - 문제 번들을 읽기 쉬운 Markdown으로 렌더링하는 공용 유틸리티
2. `scripts/curation/exportQuizBatchMarkdown.mjs` 추가
   - 기존 `.cache/quiz-bundle-batches/...` 결과를 일괄 Markdown으로 역변환
   - 세션 인덱스와 전체 세션 인덱스 생성
3. `scripts/curation/runQuizBundleBatches.mjs` 수정
   - 새 배치부터 `parsed.json`과 함께 `{model}.md`도 자동 저장
4. `package.json` 업데이트
   - `quiz:bundle:export-md` 추가

### 검증 결과
- 기존 500개 결과를 Markdown으로 변환 완료
- 생성 위치:
  - 전체 인덱스: `.cache/quiz-bundle-batches/internal-test-500/_all_sessions.md`
  - 세션 인덱스:
    - `.cache/quiz-bundle-batches/internal-test-500/20260307-195704/_session_results.md`
    - `.cache/quiz-bundle-batches/internal-test-500/20260307-200858/_session_results.md`
  - 배치 상세 예시:
    - `.cache/quiz-bundle-batches/internal-test-500/20260307-195704/batch-001-0000/gemini-3-flash-preview.md`

### 판단 근거
- 다음 단계부터는 JSON을 직접 열지 않아도, Markdown 인덱스와 배치별 `md` 파일만으로 결과를 빠르게 검토할 수 있다.
- 큐레이션 라벨링은 JSON보다 Markdown이 훨씬 빠르므로, 이후 `usable / revise / reject` 작업 속도도 좋아진다.

## [2026-03-07] [co] 내부 테스트 첫 50개 사람 검토 기준 정리

### 작업 배경
사용자가 `라벨링은 사람이 먼저 보는 게 맞다`는 방향에 동의했다. 다만 500개 전체를 바로 정독하면 느리기 때문에, 사람이 빠르게 판단할 수 있는 기준표와 `첫 50개` 검토 세트를 따로 고정할 필요가 있었다.

### 변경 내용
1. `docs/develop/35_[co]quiz_labeling_rubric.md` 추가
   - `usable / revise / reject` 기준
   - 실패 사유 코드
   - `30~60초` 빠른 검토 순서
2. `docs/develop/36_[co]internal_test_first_50_review_set.md` 추가
   - 첫 세션 `10배치 / 50엔터티`를 사람 검토용 1차 샘플 세트로 고정
   - 배치별 Markdown 링크 정리
3. `docs/develop/19_[co]frontend_mvp_checklist.md` 업데이트
   - 라벨링 기준표와 첫 50 검토 세트는 완료
   - 실제 사람 라벨링 완료 항목은 pending으로 유지

### 판단 근거
- 지금 필요한 것은 자동 큐레이션이 아니라 `실패 패턴 찾기`다.
- 따라서 `500 전체`보다 `첫 50`을 사람이 빠르게 보는 편이 실무적으로 맞다.
- 이 50개에서 실패 코드가 반복되면, 그때 프롬프트를 수정하는 것이 가장 빠르다.

## [2026-03-07] [co] 첫 15개 문제 수동 라벨링 및 인용 리스크 검토

### 작업 배경
사용자가 특히 `문장 단서 중 환상으로 만들어낸 문장`을 경계해야 한다고 짚었다. 실제 역사 지식이 없어도 플레이어가 불안하지 않게 하려면, 최소한 `첫 15개`는 사람이 직접 보고 `가짜 인용 위험`을 먼저 잡아낼 필요가 있었다.

### 변경 내용
1. `docs/develop/21_[co]ranked_clue_generation_rules.md` 업데이트
   - `contextual_line`은 실제 인용처럼 쓰지 않도록 규칙 추가
   - `lesser_known_quote` / `authentic_quote`는 출처 검증 가능할 때만 사용하도록 규칙 추가
2. `docs/develop/35_[co]quiz_labeling_rubric.md` 업데이트
   - `QUOTE_RISK` 실패 코드 추가
3. `docs/develop/37_[co]first_15_manual_label_review.md` 추가
   - 첫 15개 문제를 `usable / revise`로 나눔
   - 각 문제의 `quote_status`를 따로 기록

### 검토 결과
- 총 `15개`
- `usable 7 / revise 8 / reject 0`
- 주요 리스크:
  - `QUOTE_RISK`
  - `DIRECT_HIDDEN`
  - `TYPE_DRIFT`
  - `IMAGE_RISK`
- 핵심 발견:
  - 문제 구조 자체보다 `contextual_line`이 실제 인용처럼 보이는 경우가 많았다.
  - `lesser_known_quote`는 일부만 실제 출처와 가깝고, 일부는 사실상 만들어낸 문장에 가까웠다.

### 판단 근거
- 지금 단계의 1차 프롬프트 수정은 이미지보다 `문장 단서 규율`을 먼저 잡는 편이 효과가 크다.
- 특히 `contextual_line`을 1인칭 가짜 인용처럼 쓰지 못하게 막는 것이 가장 먼저다.

## [2026-03-07] [co] 두 번째 15개 문제 수동 라벨링 및 안내 문구 반영

### 작업 배경
사용자가 `일부 문장 단서는 실제 인용이 아닐 수 있다`는 안내를 제품 차원에서 두는 것에 동의했다. 동시에 다음 15개도 같은 기준으로 보면서, 인용 리스크가 정말 반복되는지 확인할 필요가 있었다.

### 변경 내용
1. `docs/develop/15_[co]frontend_mvp_product_plan.md` 업데이트
   - `일부 문장 단서는 실제 인용문이 아니라, 역사적 맥락을 바탕으로 재구성된 게임용 텍스트입니다.` 안내 원칙 추가
2. `docs/develop/16_[co]frontend_mvp_development_plan.md` 업데이트
   - 첫 플레이 1회 노출용 안내 문구 위치 정의 항목 추가
3. `docs/develop/38_[co]second_15_manual_label_review.md` 추가
   - `batch-004` ~ `batch-006` 수동 리뷰 기록

### 검토 결과
- 총 `15개`
- `usable 4 / revise 11 / reject 0`
- 주요 리스크:
  - `QUOTE_RISK`
  - `DIRECT_HIDDEN`
  - `TYPE_DRIFT`

### 판단 근거
- 두 번째 15개에서도 같은 문제가 반복됐다.
- 즉 현재 단계의 병목은 문제 소재보다 `문장 단서 화법`이다.
- 제품 차원의 안내 문구는 넣되, 프롬프트에서도 계속 `게임 메모 톤`을 강제하는 편이 맞다.

## [2026-03-07] [co] 첫 50개 수동 라벨링 완료 및 `lesser_known_quote` 강등 규칙 강화

### 작업 배경
사용자가 `가짜인데 진짜처럼 쓰는 문장`을 계속 경계했고, 제품 차원의 안내 문구는 유지하되 프롬프트에서도 더 강하게 막는 편이 맞다고 판단했다. 따라서 남은 20개를 마저 리뷰해 `first 50`을 닫고, `lesser_known_quote`를 예외값으로 더 강하게 낮추는 규칙을 추가했다.

### 변경 내용
1. `scripts/curation/quizBundleModelDryRun.mjs` 업데이트
   - `lesser_known_quote`를 기본값이 아니라 예외값으로 명시
   - 출처가 조금이라도 애매하면 `contextual_line`으로 강등하도록 프롬프트 강화
   - 인용문을 더 그럴듯하게 보이도록 창작하는 행위를 금지
2. `docs/develop/21_[co]ranked_clue_generation_rules.md` 업데이트
   - 같은 규칙을 운영 문서에도 반영
3. `docs/develop/39_[co]third_20_manual_label_review.md` 추가
   - `batch-007` ~ `batch-010`의 20개 수동 리뷰 기록
4. `docs/develop/40_[co]first_50_manual_label_summary.md` 추가
   - 첫 50개 전체 요약과 다음 수정 포인트 정리
5. `docs/develop/19_[co]frontend_mvp_checklist.md` 업데이트
   - `first 50` 사람 라벨링 완료 처리

### 검토 결과
- 첫 50개 총계:
  - `usable 15`
  - `revise 35`
  - `reject 0`
- 핵심 패턴:
  - `QUOTE_RISK`
  - `DIRECT_HIDDEN`
  - `IMAGE_RISK`
  - `TYPE_DRIFT`

### 판단 근거
- 파이프라인 자체는 실패하지 않았다.
- 문제 구조는 대부분 살아 있고, 실제 병목은 `문장 단서 화법`이다.
- 따라서 다음 단계는 후보 폐기가 아니라 `프롬프트 수정 후 재생성`이다.

## [2026-03-08] [cl] 카드 콘텐츠 파이프라인 완료 + AI 영화 생성 + persons 처리 방침 수립
## [2026-03-08] [co] `lesser_known_quote` 강등 후 first 15 재생성 스팟 리뷰

### 배경
### 작업 배경
- 이전 세션에서 persons 8,520건 카드 생성 완료 (Gemini 3 Flash Preview)
전날 handoff 기준 다음 단계는 `강화된 프롬프트로 larger subset 재생성`이었다. 따라서 먼저 `first 50` 재생성을 돌리고, 그중 `first 15`를 빠르게 다시 봐서 실제로 `QUOTE_RISK`가 줄었는지 확인했다.
- 이번 세션에서 final/ 재구축 (19,938 → 56,819건), 나머지 4개 카테고리 카드 생성, 영화 데이터 구축


### 변경 내용
### 작업 내용
1. `scripts/curation/runQuizBundleBatches.mjs`로 `internal-test-50-rerun` 세션 생성

   - 세션: `20260308-210631`
#### 1. generateCards.py 모델 옵션 추가
   - 범위: `10배치 / 50엔터티`
- `--model` 플래그 추가: `gemini-2.5-flash-lite` 옵션 (비용 절감)
2. `docs/develop/41_[co]rerun_first_15_spot_review.md` 추가
- 기존 Gemini 2.5 Flash 대비 ~60% 비용 절감
   - 강화된 프롬프트 결과의 첫 15개를 빠르게 재평가

3. `docs/develop/19_[co]frontend_mvp_checklist.md` 업데이트
#### 2. final/ 재구축 (56,819건)
   - 품질 개선 확인 항목 완료 처리
- 기존: 19,938건 (AI 스코어링 통과분만)

- 재구축: sl≥20 기준으로 확대 (AI 스코어링 없이 기계적 컷오프)
### 검토 결과
- persons: 8,520 → **52,229** / events: 3,523 → **1,238** / artworks: 6,790 → **2,812**
- `line_kind`: `contextual_line 50 / 50`
- inventions: 738 → **417** / items: 367 → **123**
- `first 15` 스팟 리뷰:
- ※ events/artworks/inventions/items는 재구축 시 기준 변경으로 오히려 감소
  - `usable 13`

  - `revise 2`
#### 3. 4개 카테고리 카드 생성 (Gemini 2.5 Flash Lite)
  - `reject 0`
- items: 123건, inventions: 417건 (실패 1), events: 1,238건 (실패 1), artworks: 2,812건 (실패 267)
- 남은 대표 이슈:
- 총 4,590건 카드 생성 완료
  - `명량 해전`의 `DIRECT_HIDDEN`

  - `오스만 제국`의 `TYPE_DRIFT`
#### 4. artwork 역사적 맥락 AI 필터

- 대상: sl≥30 + 날짜없음 + kowiki있음 = 6,771건
### 판단 근거
- Gemini 2.5 Flash Lite로 YES/NO 판정
- `QUOTE_RISK`는 사실상 핵심 병목에서 내려왔다.
- 결과: YES 2,393 (35.4%) / NO 4,267 / ERR 111
- 추가 미세 수정 없이도 `500 전체 재생성`으로 넘어갈 근거가 충분하다.
- → +2,393건 artworks 추가 후보 (날짜/좌표 보충 필요)

- 스크립트: `/tmp/pilotArtworkFilter.py`
## [2026-03-08] [cl] 카드 콘텐츠 파이프라인 완료 + AI 영화 생성 + persons 처리 방침 수립

#### 5. AI 영화 생성 (Wikidata 없이 순수 AI 지식)
### 배경
- **배경**: Wikidata 후보(selectFilmsByYear.py) → 일본 전대물 과다 → 순수 AI 생성으로 전환
- 이전 세션에서 persons 8,520건 카드 생성 완료 (Gemini 3 Flash Preview)
- **모델 변천**: gemini-2.5-flash-lite (연도 무시 할루시네이션) → **gemini-3.1-flash-lite-preview** (정확)
- 이번 세션에서 final/ 재구축 (19,938 → 56,819건), 나머지 4개 카테고리 카드 생성, 영화 데이터 구축
- **결과**: 1920~2025, 106개 연도, **470편** (중복 제거 후)

- **스키마**: title_ko, title_en, year, country, director, lead_actors, genre, setting_location, description
### 작업 내용
- **규칙**: 국가당 연도별 최대 2편, 평서문(~다/~했다), 배경지(setting_location) 별도 필드

- **비용**: ~$0.10, ~381K 토큰
#### 1. generateCards.py 모델 옵션 추가
- **파일**: jinserver `/mnt/data2/wikidata/output/generated_films_deduped.jsonl`
- `--model` 플래그 추가: `gemini-2.5-flash-lite` 옵션 (비용 절감)
- **deprecated**: `selected_films.jsonl` (Wikidata 기반 321편, 전대물 편향)
- 기존 Gemini 2.5 Flash 대비 ~60% 비용 절감
- 스크립트: `/tmp/generateFilmList.py`


#### 2. final/ 재구축 (56,819건)
#### 6. persons 처리 방침 수립 (핵심)
- 기존: 19,938건 (AI 스코어링 통과분만)

- 재구축: sl≥20 기준으로 확대 (AI 스코어링 없이 기계적 컷오프)
**현황 분석 결과**:
- persons: 8,520 → **52,229** / events: 3,523 → **1,238** / artworks: 6,790 → **2,812**
- final/persons.jsonl: **52,229건** (sl≥20 기계적 컷오프)
- inventions: 738 → **417** / items: 367 → **123**
- 카드 완료: **3,293건** (기존 AI 큐레이션 통과분, avg sl=85.9)
- ※ events/artworks/inventions/items는 재구축 시 기준 변경으로 오히려 감소
- 카드 미완료: **48,936건** (avg sl=35.9)


#### 3. 4개 카테고리 카드 생성 (Gemini 2.5 Flash Lite)
**연대별 편중 문제**:
- items: 123건, inventions: 417건 (실패 1), events: 1,238건 (실패 1), artworks: 2,812건 (실패 267)
| 시대 | 건수 | 비율 |
- 총 4,590건 카드 생성 완료
|------|------|------|

| 고대~근세 (~1800) | 6,544 | 13% |
#### 4. artwork 역사적 맥락 AI 필터
| 근대 (1800~1900) | 5,701 | 11% |
- 대상: sl≥30 + 날짜없음 + kowiki있음 = 6,771건
| 현대초 (1900~1950) | 10,944 | 21% |
- Gemini 2.5 Flash Lite로 YES/NO 판정
| 현대 (1950~1980) | 14,105 | 27% |
- 결과: YES 2,393 (35.4%) / NO 4,267 / ERR 111
| 동시대 (1980~) | 14,747 | 28% |
- → +2,393건 artworks 추가 후보 (날짜/좌표 보충 필요)

- 스크립트: `/tmp/pilotArtworkFilter.py`
- 55%가 1950년 이후 출생 (축구선수, 현역 배우 등 하위문화 인물 다수)

- 기존 AI 큐레이션 3,293건은 고대~근세 46% 집중 → 역사 타임라인에 적합
#### 5. AI 영화 생성 (Wikidata 없이 순수 AI 지식)

- **배경**: Wikidata 후보(selectFilmsByYear.py) → 일본 전대물 과다 → 순수 AI 생성으로 전환
**진형 결정 (2026-03-08)**:
- **모델 변천**: gemini-2.5-flash-lite (연도 무시 할루시네이션) → **gemini-3.1-flash-lite-preview** (정확)
- **최종 목표: ~20,000명** (대략 1년에 100명+)
- **결과**: 1920~2025, 106개 연도, **470편** (중복 제거 후)
- **전체 52K 무차별 처리는 비용/품질 모두 비효율 ("무식한 짓")**
- **스키마**: title_ko, title_en, year, country, director, lead_actors, genre, setting_location, description
- **단계별 진행**:
- **규칙**: 국가당 연도별 최대 2편, 평서문(~다/~했다), 배경지(setting_location) 별도 필드
  1. 기계적 사전 필터: occupation QID로 스포츠/연예인 등 필터 + 연대별 sl 차등
- **비용**: ~$0.10, ~381K 토큰
  2. AI 큐레이션: 남은 것만 카드 가치 판단
- **파일**: jinserver `/mnt/data2/wikidata/output/generated_films_deduped.jsonl`
  3. 카드 생성: 통과분만 (최종 ~2만명)
- **deprecated**: `selected_films.jsonl` (Wikidata 기반 321편, 전대물 편향)
- 비용 전략: 사전 필터로 5만→1~1.5만으로 줄인 뒤 AI 처리 ($10~20 예상)
- 스크립트: `/tmp/generateFilmList.py`

### 데이터 현황 요약 (2026-03-08)
#### 6. persons 처리 방침 수립 (핵심)

| 항목 | 건수 | 상태 |
**현황 분석 결과**:
|------|------|------|
- final/persons.jsonl: **52,229건** (sl≥20 기계적 컷오프)
| **카드 완료** | | |
- 카드 완료: **3,293건** (기존 AI 큐레이션 통과분, avg sl=85.9)
| persons 카드 | 3,293 | 완료 (Gemini 3 Flash) |
- 카드 미완료: **48,936건** (avg sl=35.9)
| events 카드 | 1,237 | 완료 (2.5 Flash Lite) |

| artworks 카드 | 2,545 | 완료 (2.5 Flash Lite, 267 실패) |
**연대별 편중 문제**:
| inventions 카드 | 416 | 완료 (2.5 Flash Lite) |
| 시대 | 건수 | 비율 |
| items 카드 | 123 | 완료 (2.5 Flash Lite) |
|------|------|------|
| AI 영화 | 470 | 완료 (3.1 Flash Lite) |
| 고대~근세 (~1800) | 6,544 | 13% |
| **소계** | **8,084** | **프론트엔드 반영 가능** |
| 근대 (1800~1900) | 5,701 | 11% |
| **대기** | | |
| 현대초 (1900~1950) | 10,944 | 21% |
| persons 미처리 | 48,936 | 단계별 필터 후 카드 생성 필요 |
| 현대 (1950~1980) | 14,105 | 27% |
| AI필터 YES artworks | 2,393 | 날짜/좌표 보충 + 카드 생성 필요 |
| 동시대 (1980~) | 14,747 | 28% |
| **프론트엔드 변환** | | |

| MockEvent 변환 | - | items/inventions/events/artworks 미수행 |
- 55%가 1950년 이후 출생 (축구선수, 현역 배우 등 하위문화 인물 다수)
| artworks 좌표 보강 | - | SPARQL P1071/P840/creator P19 |
- 기존 AI 큐레이션 3,293건은 고대~근세 46% 집중 → 역사 타임라인에 적합

### 잔여 작업 (다음 세션)
**진형 결정 (2026-03-08)**:
1. **persons 단계별 필터 설계 + 실행** ← 최우선
- **최종 목표: ~20,000명** (대략 1년에 100명+)
2. AI필터 YES artworks 2,393건 날짜/좌표 보충
- **전체 52K 무차별 처리는 비용/품질 모두 비효율 ("무식한 짓")**
3. 완료된 카드 → MockEvent 변환 (프론트엔드 통합)
- **단계별 진행**:
4. 영화 470편 좌표 매핑 (setting_location → lat/lon)
  1. 기계적 사전 필터: occupation QID로 스포츠/연예인 등 필터 + 연대별 sl 차등
  2. AI 큐레이션: 남은 것만 카드 가치 판단
  3. 카드 생성: 통과분만 (최종 ~2만명)
- 비용 전략: 사전 필터로 5만→1~1.5만으로 줄인 뒤 AI 처리 ($10~20 예상)

### 데이터 현황 요약 (2026-03-08)

| 항목 | 건수 | 상태 |
|------|------|------|
| **카드 완료** | | |
| persons 카드 | 3,293 | 완료 (Gemini 3 Flash) |
| events 카드 | 1,237 | 완료 (2.5 Flash Lite) |
| artworks 카드 | 2,545 | 완료 (2.5 Flash Lite, 267 실패) |
| inventions 카드 | 416 | 완료 (2.5 Flash Lite) |
| items 카드 | 123 | 완료 (2.5 Flash Lite) |
| AI 영화 | 470 | 완료 (3.1 Flash Lite) |
| **소계** | **8,084** | **프론트엔드 반영 가능** |
| **대기** | | |
| persons 미처리 | 48,936 | 단계별 필터 후 카드 생성 필요 |
| AI필터 YES artworks | 2,393 | 날짜/좌표 보충 + 카드 생성 필요 |
| **프론트엔드 변환** | | |
| MockEvent 변환 | - | items/inventions/events/artworks 미수행 |
| artworks 좌표 보강 | - | SPARQL P1071/P840/creator P19 |

### 잔여 작업 (다음 세션)
1. **persons 단계별 필터 설계 + 실행** ← 최우선
2. AI필터 YES artworks 2,393건 날짜/좌표 보충
3. 완료된 카드 → MockEvent 변환 (프론트엔드 통합)
4. 영화 470편 좌표 매핑 (setting_location → lat/lon)

---

## [co] 2026-03-08 22:15 KST - 퀴즈 이미지 파일럿 공개 폴더 저장

### 작업 내용
- `scripts/curation/runQuizImagePilot.mjs` 추가
  - 최신 `internal-test-50-rerun` parsed bundle에서 `image_clues`를 직접 읽어 Gemini 이미지 모델로 생성
  - 결과를 비숨김 경로 `public/quiz-image-pilot/<timestamp>/`에 저장
  - 원본 PNG, 512 축소본, raw JSON, `index.md` 동시 생성
- `data/curation/image_pilots/rerun_first15_batch01.json` 추가
  - 1차 이미지 검증용 8개 엔터티 세트 정의
- `package.json`에 `quiz:image-pilot` 스크립트 추가
- 파일명 충돌 문제 수정
  - 한국어 정답명이 비어 `_card1.png`로 덮이던 문제를 index prefix로 회피

### 실행 결과
- 성공 폴더:
  - `public/quiz-image-pilot/2026-03-08T13-12-24-311Z/`
- 대상 8개:
  - 세종대왕, 만사 무사, 요하네스 구텐베르크, 이순신, 이븐 알하이삼, 마리 퀴리, 알렉산드리아, 징기스칸
- 목적:
  - `500` 전체 재생성보다 앞서, 개선된 문제 번들이 실제 이미지 카드로 성립하는지 먼저 검증

### 다음 액션
1. `public/quiz-image-pilot/2026-03-08T13-12-24-311Z/index.md` 기준으로 사용자 검토
2. `direct generate OK / prompt revise / reference needed`로 8개 분류
3. 이미지 검증 결과를 본 뒤 `500` 재생성 재개 여부 결정

## [co] 2026-03-08 22:35 KST - 퀴즈→이미지 검증 루프 고정 및 이순신 재생성

### 작업 내용
- `docs/develop/42_[co]quiz_to_image_validation_pipeline.md` 추가
  - 현재 내부 테스트용 실제 루프를 `문제 생성 -> 수동 리뷰 -> 이미지 프롬프트 조립 -> 이미지 생성 -> 결과 리뷰`로 정리
- `scripts/curation/runQuizImagePilot.mjs` 개선
  - 엔터티별 `context` / `negative override` 지원
  - manifest 값보다 CLI 인자가 우선되도록 수정
  - 한국어 정답명 파일명 충돌 회피
- `data/curation/image_pilots/rerun_first15_batch01.json`에 문화권/시대 앵커 추가
  - 특히 `이순신`, `세종대왕`, `마리 퀴리`, `징기스칸`에 오인 방지 문구 추가

### 실행 결과
- 전체 8장 공개 폴더 저장:
  - `public/quiz-image-pilot/2026-03-08T13-12-24-311Z/`
- 이순신 단건 재생성:
  - `public/quiz-image-pilot/2026-03-08T13-29-24-165Z/`

### 판단
- 이순신은 `late 16th century Joseon Korea naval setting` 같은 문화권 앵커를 넣자 개선됨
- 이미지 단계의 핵심 병목은 개별 프롬프트 수정보다 `오인 리스크를 규칙화해서 자동 조립`하는 쪽으로 정리됨

## [co] 2026-03-08 23:05 KST - first 50 무개입 이미지 파일럿 실행

### 작업 내용
- `data/curation/image_pilots/rerun_first50_clue1_no_override.json` 추가
  - `internal-test-50-rerun`의 첫 50개 정답 엔터티를 그대로 이미지 파일럿에 넣는 무개입 매니페스트
- `scripts/curation/runQuizImagePilot.mjs`에 진행 로그 추가
  - `[n/50] generating ...`, `[n/50] saved ...`
- `prompt override` 없이 현재 문제 번들만으로 `clue_index=0` 이미지 생성 실행

### 실행 결과
- 성공 폴더:
  - `public/quiz-image-pilot/first50-no-overrides/2026-03-08T14-03-16-370Z/`
- 결과:
  - `50 / 50` 성공
  - 원본 PNG, `512px`, raw JSON, `index.md` 생성 완료
  - 아침 검토용 `gallery.html`, `gallery.md` 생성 완료
  - 고정 진입점 `public/quiz-image-pilot/first50-no-overrides/latest.html` 생성 완료

### 의미
- 텍스트 프롬프트 단계는 개입 없이도 이미지 생성까지 안정적으로 이어짐을 확인
- 이제 병목은 생성 실패가 아니라 `문화권/시대/성별 오인` 같은 품질 판정과 규칙화로 이동

### 다음 액션
1. `first50-no-overrides` 결과를 빠르게 훑어 실패 패턴 코드화
2. `context/negative override` 없이도 필요한 최소 메타 규칙 도출
3. 그 규칙으로 500 재생성 재개 여부 판단

## [co] 2026-03-09 07:35 KST - [gm][Feat] 10개 파일럿 이미지 생성 테스트 완료 (Gemini 2.5 Flash Image) 및 리뷰 UI 연동
- [gm][Feat] 10개 추가 파일럿 이미지 비교 테스트 수행 (Gemini 3.1 Flash Image)
- [gm][Fix] 리뷰 UI에서 V1, V2 이미지를 라벨과 함께 구분하여 표시하도록 업데이트

### 작업 내용
- `scripts/curation/buildCuratedMustHave50Candidates.mjs` 추가
  - [33_[co]internal_test_must_have_50_candidates.md] 기준으로 `internal_test_curated_50_candidates.json` 생성
  - 기존 `internal_test_500_candidates.json` 앞 50개를 쓰던 잘못된 테스트 경로를 폐기
- `scripts/curation/runQuizImagePilot.mjs` 수정
  - 기본 모델을 `gemini-2.5-flash-image`로 변경
  - 저장 포맷을 원본+축소본 2장 방식에서 `500px` 단일 결과물로 변경
  - 이미지 실패 시 전체 런이 죽지 않고 failure 로그만 남기고 계속 진행하도록 수정
- `scripts/curation/buildQuizImagePilotManifest.mjs` 추가
  - 퀴즈 배치 세션과 candidate 파일을 받아 이미지 파일럿 manifest 생성
- 새 curated 50 퀴즈 생성 실행
  - 세션: `.cache/quiz-bundle-batches/internal-test-curated-50/20260309-070123/`
  - 결과: `50 / 50` 배치 생성 완료
- `gemini-2.5-flash-image`로 curated 50 이미지 파일럿 실행
  - 메인 런: `public/quiz-image-pilot/curated50-no-overrides/2026-03-08T22-18-36-637Z/` (`42 / 50` 성공 후 `방콕`에서 텍스트 응답 반환)
  - tail 런: `public/quiz-image-pilot/curated50-no-overrides-tail/2026-03-08T22-28-31-007Z/` (`8 / 8` 성공)
  - 합본 진입점: `public/quiz-image-pilot/curated50-latest.md`
- 비용 비교 문서 추가
  - [43_[co]image_generation_cost_comparison.md](/Users/jin/Documents/development/TimeGlobe/docs/develop/43_%5Bco%5Dimage_generation_cost_comparison.md)

### 판단
- 비용 기준으로는 공식 Gemini Batch가 APIYI보다 유리함
- 내부 테스트용 표준 호출은 `gemini-2.5-flash-image`로 충분히 빠름
- 다만 일부 프롬프트에서 이미지 대신 텍스트가 반환될 수 있어 재시도 로직은 필요
- 가장 중요한 개선은 모델 변경보다 `curated 50` 자체를 정확히 고정한 것

---

## [2026-03-09] [co] 프로젝트 재점검용 종합 handoff 문서 재작성

### 작업 배경
- 세션 저장 없이 종료된 뒤, 기존 handoff가 `persons` 중심 문서 하나로만 남아 있어 전체 프로젝트를 다시 점검하기엔 컨텍스트가 분산돼 있었다.
- 사용자 요청은 `왜 시작했고`, `무슨 요구가 있었고`, `무슨 작업을 거쳤고`, `현재 파이프라인과 테스트 결과물이 어디에 있는지`를 한 번에 볼 수 있는 종합 handoff 문서를 오늘 날짜로 다시 정리하는 것이었다.

### 변경 내용
1. `docs/handoff_20260309_project_reassessment.md` 추가
   - 최근 지도/UI 안정화, `persons_cards.json` 프론트 반영, `500 entity` 퀴즈 파이프라인, 이미지 파일럿, `persons 52K` 후속 전략까지 한 문서로 재정리
   - 사용자 요구, 작업 연표, 현재 파이프라인, 산출물 경로, 열린 리스크, 권장 재시작 순서를 포함
2. 현재 워크트리/원격 상태를 handoff에 반영
   - `HEAD == dev/main`
   - 다만 수정/미추적 파일이 많아 pull보다 워크트리 정리와 우선순위 재선정이 먼저라는 점 명시

### 판단 근거
- 이제 필요한 문서는 단일 task resume용 handoff가 아니라, 프로젝트 전체를 다시 스캔하고 방향을 고를 수 있는 `재점검용 handoff`다.
- 결과물 폴더와 실제 실행 경로를 함께 적어두지 않으면 다음 세션에서 다시 탐색 비용이 커진다.

### 2026-03-09 [gm] 퀴즈 생성 로직 고도화 및 데이터 재생성 완료 (암시적 힌트 강화)
* **목록**: `validation_temp.json`, `generate_50.mjs`, `add_ko_prompts.mjs`
* **내용**: 
  - 정답을 표면적으로 설명하던 방식(고유명사 남발 등을 배제하고, 시대적/지역적 배경을 나타내는 '암시적 힌트'를 주도록 프롬프트(SYSTEM_PROMPT) 전면 재수정
  - 단서 문장을 명사형으로 끝내지 않고 반드시 평서문(~이다, ~했다)으로 끝맺도록 보완
  - 50개 퀴즈 타겟에 대해 `gemini-3.1-pro-preview`를 사용하여 신규 데이터셋 `validation_temp.json` 생성 및 검합 완료.

### 🗓️ 2026-03-09 21:50 [gm]
- **작업명**: 50개 검증 셋 전체 이미지 생성 및 UI 통합 완료 (최종)
- **내용**: 
    - 나머지 30개 검증 아이템에 대해 `Gemini 3.1 Flash Image` 모델(512px)로 유료 키를 사용하여 이미지 생성 완료.
    - `run_validation_images_v2.mjs`에 병렬 청크 처리(Chunk size: 5) 로직을 추가하여 생성 속도와 안정성 확보.
    - 50개 전체 아이템의 이미지를 `public/quiz-image-pilot-v2/final` 경로에 글로벌 인덱스(`item_XX_512.png`) 형식으로 통합.
    - 관리자 리뷰 UI(`src/app/admin/quiz-reviewer/page.tsx`)를 수정하여 V1/V2 배지 자동 노출 및 512px 이미지 렌더링 최적화.
    - **결과**: 49/50 성공 (1건 안전 필터 실패). 512px 화질이 게임 리소스로 충분함을 확인하여 향후 1만 장 생성 시 비용 절감 전략(Paid Batch 50% 할인 + Free Tier 병행) 수립 기반 마련.
